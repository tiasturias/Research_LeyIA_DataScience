# Plan Detallado Fase 6 — Modelado MVP (versión 0.4)

**Proyecto:** Research_AI_law — Boletín 16821-19 Ley Marco IA Chile
**Versión objetivo:** `fase6-v0.4`
**Tag git objetivo:** `fase6-v0.4`
**Directorio raíz:** `/home/pablo/Research_LeyIA_DataScience/Research_AI_law/F5_F8_MVP/FASE6/`
**Fase CRISP-DM:** Paso 4 — Modeling
**Fecha de plan:** 2026-05-07 (revisión 0.4)
**Bases robustas previas:** `FASE3/` (Matriz Madre v1.1), `FASE4/` (EDA v1.0 con análisis exploratorio de varianza/redundancia), `FASE5/` v2.0 (Data Preparation con bundle `phase6_ready/` de **12 archivos**, 46 vars)
**Plan maestro de referencia:** `CONTEXTOS/5.PLAN_FASE_MVP_END_TO_END.md` (§6.7-§6.10, §7.1.2, §12.2)
**Pre-requisito de ejecución:** `CONTEXTOS/5.0.PLAN_FASE5_V2_UPDATE.md` debe estar 100% ejecutado antes de iniciar Fase 6.

## Cambios v0.1 → v0.2 (revisión 2026-05-07)

- **+Q5** Uso de IA por la población (Anthropic-based, 3 Y).
- **+Q6** Uso de IA en sector público (Oxford + OECD, 5 Y primarias + 1 auxiliar).
- **Q2 mantiene todas sus Y**, Q5/Q6 reusan algunas Y (overlap permitido).
- **Expansión metadata Fase 5**: agregar 6 variables nuevas al MVP (40 → 46 vars), regenerar bundle `phase6_ready/`. NO recompute de Fase 3 ni de transformaciones existentes.
- **+3h** sobre el plan original (8h → 11h total).

## Cambios v0.3 → v0.4 (revisión 2026-05-07, eliminación de scope PCA)

- **Reducción dimensional (PCA) eliminada del scope de Fase 6.** Análisis exploratorio de varianza/redundancia ya cubierto por Fase 4 (`eda_redundancy_report.csv`, `eda_inter_block_correlations.csv`, `eda_candidates_for_feature_engineering.csv`).
- **Justificación técnica:** ratio N/p ≈ 0.4 (43 países, ~108 features post-z) hace PCA inestable (Costello & Osborne 2005 recomienda N≥5p); Q4 clustering, Ridge y Lasso cubren la función analítica relevante; PC components no son interpretables para la audiencia política (Senado de Chile).
- **No se crea `_common_diagnostics.py`**, no se generan outputs `pca_*.csv`, no se introduce `test_pca_diagnostics.py`. Si Fase 7 requiere PCA como sensitivity check exploratorio, se ejecuta como apéndice — nunca como input de modelos primarios.
- **Fase 6 outputs:** **20 archivos** (Q1: 4, Q2: 2, Q3: 2, Q4: 5, Q5: 3, Q6: 3, manifest: 1; sin archivos PCA).
- **Tiempo total Fase 6:** 11h → **9h** (eliminado paso PCA load+verify).
- **Backwards compatible** con v0.2 en todo lo demás (Q1-Q6 sin cambios estructurales).

---

## 0. Contexto y propósito

### 0.1 Por qué Fase 6 ahora

Fase 5 cerró con un bundle `phase6_ready/` de 12 archivos (87.2 KB matriz + 11 metadatos), validado contra Fase 3 con 74 hashes, y entregado vía API estable `FASE5.src.api.load_phase6_*`. Esta fase es la primera que **modela** la asociación entre regulación de IA y desarrollo del ecosistema.

### 0.2 Hipótesis principal a responder

> ¿Existe una asociación estadísticamente significativa entre las características de la regulación de IA de un país y el desarrollo de su ecosistema de IA, después de controlar por factores socioeconómicos e institucionales?

**Audiencia primaria:** Jefe del proyecto (no técnico) y eventualmente Senado de Chile.
**Audiencia secundaria:** Auditores metodológicos.

### 0.3 Sub-preguntas operativas (Q1-Q6)

| Sub-pregunta | Tipo | Output | Métricas principales |
|---|---|---|---|
| **Q1 Inversión** | Regresión | Coeficientes + IC + bootstrap | β, IC95 bootstrap, p-FDR |
| **Q2 Adopción** (empresarial general) | Clasificación | Probabilidad + AUC | AUC 5-fold, AUC LOOCV, feature importance |
| **Q3 Innovación** | Regresión | Coeficientes + IC | R², β, IC95 bootstrap |
| **Q4 Contenido regulatorio** | Clustering | Etiquetas país × cluster | Silhouette, dendrograma |
| **Q5 Uso IA población** *(NUEVO)* | Regresión + Clasificación | β + AUC | β IC95, consistency table |
| **Q6 Uso IA sector público** *(NUEVO)* | Regresión + Clasificación | β + AUC | β IC95, consistency table |

**Solapamiento de variables Y entre sub-preguntas (decisión M):**
- Q2 mantiene sus 9 Y originales (incluye Anthropic, Oxford emerging tech, public_sector_adoption).
- Q5 reusa `anthropic_usage_pct`, `anthropic_collaboration_pct`, `oxford_ind_adoption_emerging_tech` de Q2.
- Q6 reusa `oxford_public_sector_adoption` de Q2 + 4 variables nuevas exclusivas.
- Esto refleja que las variables son conceptualmente reusables; los modelos se entrenan separadamente por sub-pregunta.

### 0.4 Lo que Fase 6 SÍ entrega (v0.4)
- **6 sub-modelos terminados** (Q1-Q6) con outputs auditables en formato long.
- 17+ modelos entrenados (OLS×6 + Ridge + Lasso + bootstrap + Logit + RF + Ridge×2 + GBR×2 + KMeans×2 + HCA×2 + PSM exploratorio + Q5 OLS + Q5 Logit + Q6 OLS + Q6 Logit).
- Tabla de **consistencia de signo** por sub-pregunta cruzando todos los Y (Q1, Q3, Q5, Q6).
- Notebook auditable cell-by-cell (`06_modeling.ipynb`) que NO recomputa modelos, solo exhibe outputs.
- Manifest con git_sha + hashes de cada output.

### 0.5 Lo que Fase 6 NO entrega (queda para Fase 7-8)
- Sensitivity analysis completo (excluyendo USA, AI leaders, Stanford) — **eso es Fase 7**.
- Comparación con baselines (modelo trivial, solo-controles) — **eso es Fase 7**.
- Hyperparameter tuning vía GridSearch — **solo verificación ex-post en Fase 7**.
- Reporte ejecutivo + Excel final + presentación al jefe — **eso es Fase 8**.
- Inferencia causal robusta (DiD, Synthetic Control, IV) — **post-MVP**.

---

## 1. Realidad empírica de los datos (verificada contra bundle)

Antes de fijar las decisiones, fijo los N reales del bundle. **Varios supuestos del plan maestro eran optimistas o pesimistas; este plan corrige.**

### 1.1 Subset IAPP (cuello de botella estructural)
- 18/43 países tienen datos IAPP completos (no 28).
- De los 18: **6 con `iapp_ley_ia_vigente=1`** (no 9), **12 con `iapp_ley_ia_vigente=0`**.
- Implicación: PSM 1:1 con caliper estricto producirá 2-5 pares — insuficiente para inferencia causal sola.

### 1.2 N efectivo por sub-pregunta (verificado)

| Sub-pregunta | Y representativo | X1 incluida | N efectivo (post-dropna) |
|---|---|---|---|
| Q1 | `oxford_ind_company_investment_emerging_tech` | `n_binding`, `n_non_binding` | **36** (X1 agregadas, no IAPP raw) |
| Q1 | mismo Y | IAPP raw (`iapp_ley_ia_vigente`) | **17** |
| Q2 | `ms_h2_2025_ai_diffusion_pct` | X1 agregadas | **41** |
| Q2 | `oecd_5_ict_business_oecd_biz_ai_pct` | X1 agregadas | **31** |
| Q3 | `wipo_out_score` | X1 agregadas | **42** |
| Q3 | `stanford_..._volume_of_publications` | X1 agregadas | **26** |
| Q4 | n/a (clustering) | features mixtos | 43 (oficial) |

**Conclusión clave:** Si usamos **agregadas regulatorias** (`n_binding`, `n_non_binding`, `n_hybrid`, `regulatory_intensity`, `n_regulatory_mechanisms` — todas 0% missing) en lugar de IAPP raw como X1 principal, recuperamos N=31-42 en lugar de N=17-25. Esta es la decisión más impactante del plan.

### 1.5 Expansión de metadata Fase 5 para Q5 y Q6 (v0.2)

Para hacer Q6 robusto y Q5 cubierto, agregamos **6 variables nuevas** al MVP que ya existen en la Matriz Madre Fase 3 v1.1 pero quedaron fuera por curaduría inicial. **No es rerun de Fase 5 data preparation; es expansión metadata + regeneración del bundle.**

| Variable nueva | Cobertura MVP_v1 | Sub-pregunta | Razón inclusión |
|---|---|---|---|
| `oxford_e_government_delivery` | 43/43 | Y_Q6_public_sector | Entrega de gobierno electrónico — proxy directo de IA en sector público |
| `oxford_government_digital_policy` | 43/43 | Y_Q6_public_sector | Política digital gubernamental |
| `oxford_ind_data_governance` | 43/43 | Y_Q6_public_sector | Gobernanza de datos del sector público |
| `oxford_governance_ethics` | 43/43 | Y_Q6_public_sector | Ética en gobernanza pública IA |
| `oecd_2_indigo_oecd_indigo_score` | 43/43 | Y_Q6_public_sector | OECD INDIGO — índice de gobernanza inteligencia digital |
| `oecd_4_digital_gov_oecd_digital_gov_overall` | 31/43 | Y_Q6_public_sector_aux | OECD digital gov score (auxiliary tier por cobertura) |

**Acciones requeridas en `FASE5/`:**
1. Actualizar `FASE5/config/mvp_variables.yaml`: 40 → 46 variables.
2. Re-ejecutar `FASE5/src/build.py` para regenerar:
   - `feature_matrix_mvp.csv`: 43 × 126 → 43 × 138 (más columnas observed_core + sus z-scores + algunos logs).
   - `MVP_AUDITABLE.xlsx`: actualizar hojas 5_Variables_40 → 5_Variables_46 y 6_Matriz_40_Humana → 6_Matriz_46_Humana.
   - Bundle `phase6_ready/`: regenerar los 12 archivos con nuevos hashes.
3. Actualizar `phase6_modeling_contract.yaml`:
   - Agregar `Y_Q5_population_usage` con 3 vars.
   - Agregar `Y_Q6_public_sector` con 5 vars primarias + 1 auxiliary.
   - Mantener Q2 con sus 9 Y (decisión M).
4. Actualizar tag versión Fase 5: `1.0` → `1.1` (minor, backwards compatible: superset de columnas).

**Lo que NO cambia en Fase 5:**
- Las 40 variables originales y sus transformaciones existentes — preservadas byte-a-byte.
- El split train/test 34/9 — congelado.
- Los 74 hashes contra Fase 3 — idénticos (no se toca Fase 3).
- La submuestra de 43 países — sin cambio.
- Las decisiones metodológicas firmadas — válidas.

**Tiempo estimado de la expansión Fase 5:** 2 horas (mostly regeneración del bundle, reuso código existente).

### 1.3 Anomalía train/test split — descartada

Verificado: `phase6_train_test_split.csv` tiene 34 train + 9 test = 43 sin overlap. El reporte previo de "8 países en ambos splits" fue artifact de parsing CSV (la columna `country_name_canonical` contiene comas como `"Netherlands (Kingdom of the)"`). Sin acción correctiva. Test agregado para evitar regresión.

### 1.4 Variables binarias IAPP con MAD=0
- `iapp_ley_ia_vigente_z`, `iapp_proyecto_ley_ia_z`: MAD=0, z-score no calculable.
- Acción: Fase 6 usa versión **raw** de estas binarias, NO la `_z`. Documentado en transform_params Fase 5.

---

## 2. Decisiones técnicas firmadas (A-J)

### Decisión A — Orden de ejecución
**A3 modificada: Q4 → Q1 → Q3 → Q2 → PSM (auxiliar al final).**

Razones:
1. **Q4 primero**: self-contained (sin Y), 0% missing en features, produce etiquetas de cluster reusables como contexto.
2. **Q1 segundo**: el más complejo (6 Y, 4 modelos, bootstrap, PSM). Consolida las utilidades comunes de regresión que Q3 reusa.
3. **Q3 tercero**: reusa `_common_regression` con cambio mínimo de Y. Tier separation primary vs auxiliary (Stanford).
4. **Q2 cuarto**: clasificación introduce binarización + AUC + ROC, patrón distinto.
5. **PSM al final**: solo para Q1, marcado como exploratorio.

### Decisión B — PSM con N pequeño
**B4 + B1: bootstrap-OLS como motor principal de Q1; PSM degradado a "exploratorio".**

- Bootstrap-OLS (B=2000, BCa) sobre `n_binding` y `iapp_ley_ia_vigente` → IC95 robustos para N=34-42.
- PSM con caliper **0.20** (no 0.10): producirá 3-6 pares. Si k<5 marcar `status="insufficient_n"`.
- Reporte ejecutivo: "asociación robusta a controles X2"; PSM como prueba auxiliar de robustez con sus supuestos explícitos.

### Decisión C — Stanford en Q3
**C3: dos análisis paralelos.**
- **Q3 primary**: 6 Y (sin Stanford). N=40-42 por Y.
- **Q3 auxiliary**: solo `stanford_..._volume_of_publications`. N=26. Etiquetado `tier="auxiliary_low_n"`.
- Tabla consistency cruza los 7 Y para mostrar si Stanford rompe el patrón.

### Decisión D — Cross-validation
**D4 modificada: Repeated K-fold (5×10) + Bootstrap (2000) + LOOCV sanity check.**

- **Repeated 5-fold estratificado por región** (50 folds) → CV R² / AUC con menor varianza.
- **Bootstrap 2000** → IC95 de coeficientes (no asintóticos).
- **LOOCV** → comparación contra 5-fold; si divergen mucho, hay inestabilidad estructural a documentar.

### Decisión E — Y_Q1 multivariada vs separada
**E1 + E4: modelos separados por Y + tabla de consistencia de signo + FDR.**

- 5-6 modelos OLS separados (uno por Y).
- Tabla `q1_consistency.csv`: por cada X1, contar n_y_negativo, n_y_positivo, n_y_significativo_05, n_y_significativo_05_post_FDR.
- Etiqueta `direction_summary ∈ {robust_negative, robust_positive, mixed, null}` por reglas explícitas.

### Decisión F — Hyperparameter tuning
**F1 producción + F3 verificación ex-post (Fase 7).**

Hyperparams **fijos** en Fase 6:
- Ridge `alpha=1.0`
- Lasso `alpha=0.1, max_iter=10000`
- RandomForest `n_estimators=300, max_depth=4, min_samples_leaf=3, random_state=42`
- GradientBoosting `n_estimators=200, max_depth=3, learning_rate=0.05, random_state=42`
- LogisticRegression `penalty="l2", C=1.0, max_iter=1000, class_weight="balanced"`

GridSearchCV solo en Fase 7 como **prueba de estabilidad**, no para selección.

### Decisión G — Q4 clustering N=43 vs N=18
**G3: ambos clusterings, N=43 oficial + N=18 complementario.**

- **Cluster N=43 oficial**: features = 9 one-hots IAPP + 5 agregados regulatorios (14 features mixtos, 0% missing). Distancia = **Gower** (mixed types) o Euclidean sobre features estandarizadas. Los 25 países sin IAPP visible forman cluster legítimo "sin perfil regulatorio IAPP".
- **Cluster N=18 complementario**: solo subset IAPP, vector binario IAPP puro, distancia **Jaccard**. Caracterización fina del subset regulado.

### Decisión H — Train/test split
**H3 confirmado, sin acción correctiva.** Sólo agregar `test_split_integrity.py` para prevenir regresión.

### Decisión I — Lenguaje causal
**I1 estricto en CSVs + I2 en Executive Summary + I3 condicional para PSM.**

- **CSVs**: columnas `coefficient`, `ci95_lower/upper`, `p_value`, `n_effective`. Nunca "efecto", "impacto", "causa".
- **Executive Summary** (Fase 8): "controlando por X2, observamos una asociación de magnitud β=−0.X (IC95 [−Y, −Z], p=W)".
- **Sección PSM** (si k≥5): "ATT exploratorio bajo supuestos {ignorability, common support, SUTVA}".

### Decisión J — Output schemas
**Long-format normalizado, una fila por (sub-pregunta, modelo, Y, X), con metadata de N y método.**

Detalle exhaustivo en §6 (Output Schemas).

### Decisión K — Q6 estrategia (v0.2)
**Opción B aprobada: expandir Fase 5 con 6 variables nuevas para hacer Q6 robusta.**

- Q6 final: 5 Y primarias con 100% cobertura (43/43) + 1 Y auxiliar (31/43, tier `auxiliary_low_n`).
- Justificación: con solo `oxford_public_sector_adoption` Q6 quedaría sin consistency table; el costo de expandir Fase 5 (+2h) es bajo vs. el valor metodológico (Q6 robusta defensible).
- Trade-off documentado en §1.5 — la matriz core no cambia, solo se agregan columnas.

### Decisión L — Reducción dimensional (PCA) fuera de scope (v0.4)
**PCA NO se ejecuta en Fase 6 ni en Fase 5.** Análisis exploratorio de varianza/redundancia delegado a Fase 4 (ya cerrada).

Justificación:
- **Cobertura previa:** Fase 4 ya produjo `eda_redundancy_report.csv` (1,155 pares con |ρ|≥0.85), `eda_inter_block_correlations.csv` y `eda_candidates_for_feature_engineering.csv`. Re-ejecutar PCA sería duplicar trabajo descriptivo ya validado y firmado.
- **Inestabilidad estadística:** ratio N/p ≈ 0.4 (N=43, p≈108 features post-z-score). Costello & Osborne (2005) recomienda N ≥ 5p; estamos 12× por debajo → primeras componentes capturarían ruido aleatorio de muestreo.
- **Redundancia metodológica:** Q4 (KMeans+HCA, Gower N=43 + Jaccard N=18) ya captura estructura latente; Ridge/Lasso (Q1, Q3) manejan colinealidad supervisada; Random Forest (Q2) es invariante a multicolinealidad.
- **Defensibilidad ante audiencia política:** PC1 = combinación lineal de ~108 features no tiene interpretación legible para el Senado. Modelos OLS con coeficientes interpretables ("una unidad más de `n_binding` se asocia con −X% en inversión") respetan el criterio de interpretabilidad del `pipeline_datascience.md` Paso 4 §6.
- **Investigator degrees of freedom:** Diagnóstico extra abre más caminos de "garden of forking paths" (Gelman & Loken 2014); el FDR-BH planificado no protege contra esto.

**Implicación práctica:** Fase 6 NO crea `_common_diagnostics.py`, NO genera CSVs `pca_*`, NO incluye sección PCA en notebook 06. Si en Fase 7 (sensitivity) un revisor académico exige PCA, se ejecuta como apéndice exploratorio aislado, sin contaminar la inferencia primaria.

### Decisión M — Q2/Q5/Q6 reuso de Y (v0.2)
**Q2 mantiene sus 9 Y. Q5 y Q6 reusan algunas; cada modelo se entrena por su sub-pregunta.**

- `anthropic_usage_pct` y `anthropic_collaboration_pct` aparecen como Y en Q2 Y en Q5 (sin duplicar fila en CSV — la sub-pregunta es la clave del registro).
- `oxford_public_sector_adoption` aparece como Y en Q2 Y en Q6.
- `oxford_ind_adoption_emerging_tech` aparece como Y en Q2 Y en Q5.
- En `phase6_modeling_contract.yaml`, una variable puede aparecer en múltiples roles. Los módulos de Fase 6 filtran por sub-pregunta + rol.
- Razón: las variables son conceptualmente reusables (uso ciudadano forma parte de adopción general; adopción sector público también). Forzar exclusividad sería artificial.

---

## 3. Inputs disponibles

### 3.1 Bundle Fase 5 (`phase6_ready/`) — INPUT PRINCIPAL

```
F5_F8_MVP/FASE5/outputs/phase6_ready/
├── phase6_feature_matrix.csv          ★ 43 × 138 — la matriz
├── phase6_schema.csv/.json            ★ documentación de cada columna
├── phase6_column_groups.yaml          ★ agrupación funcional
├── phase6_modeling_contract.yaml      ★ roles X1/Y/X2 + reglas
├── phase6_transform_params.csv        ★ parámetros log + z-score
├── phase6_train_test_split.csv        ★ split congelado 34/9
├── phase6_missingness_by_column.csv   ★ diagnóstico cobertura
├── phase6_missingness_by_country.csv
├── phase6_variables_catalog.csv       ★ catálogo de las 46 vars
├── phase6_llm_context.json            ★ reglas duras + secuencia de carga
└── phase6_ready_manifest.json         ★ hashes SHA-256 de los 11 anteriores
```

### 3.2 API estable

```python
from FASE5.src.api import (
    load_phase6_feature_matrix,    # → DataFrame 43×138
    load_phase6_schema,             # → DataFrame 138 filas (documentación)
    load_phase6_column_groups,      # → dict (id_cols, observed_core_46, ...)
    load_phase6_modeling_contract,  # → dict (roles, sub-preguntas, coverage)
    load_phase6_ready_manifest,     # → dict (hashes para validar integridad)
    load_phase6_llm_context,        # → dict (hard_rules + minimum_load_sequence)
)
```

### 3.3 API Fase 4 (referencia descriptiva, NO computacional)

```python
from fase4.api import (
    load_chile_profile,        # eda_chile_profile.csv — para sección Chile
    load_chile_vs_peers,       # comparativa LATAM
    load_redundancy_report,    # 1,155 pares con |ρ|≥0.85 — awareness colinealidad
)
```

Estos archivos se leen **solo para narrativa descriptiva** en el notebook. No se usan como features.

### 3.4 Reglas duras heredadas (hard rules)

De `phase6_llm_context.json`:
- `do_not_recompute_phase5: true`
- `do_not_impute_in_phase6: true`
- `do_not_mutate_phase3_or_phase4: true`
- `preserve_missingness_for_phase6_model_selection: true`
- `human_audit_excel_is_not_model_input: true`

---

## 4. Arquitectura de directorios

```
F5_F8_MVP/FASE6/
├── README.md                           # inputs, outputs, decisiones A-J, cómo correr
├── pyproject.toml                      # paquete `fase6`, dependencias
├── Makefile                            # `make fase6-all`, `make fase6-test`
├── config/
│   ├── fase6_models.yaml               # hyperparams fijos (Decisión F1)
│   ├── fase6_decisions.yaml            # decisiones A-J firmadas (humano)
│   └── fase6_psm.yaml                  # caliper, n_boot, treatment_col
├── src/
│   ├── __init__.py
│   ├── api.py                          # API estable para consumo Fase 7-8
│   ├── _common_data.py                 # load_phase6_bundle, validate_bundle_hash
│   ├── _common_regression.py           # fit_ols_with_bootstrap, fdr_correction
│   ├── _common_classification.py       # binarize_by_median, fit_logreg, fit_rf
│   ├── q1_investment.py                # OLS + Ridge + Lasso + bootstrap + PSM
│   ├── q2_adoption.py                  # Logistic + RF + AUC 5-fold + LOOCV
│   ├── q3_innovation.py                # Ridge + GBR, primary vs auxiliary tier
│   ├── q4_clustering.py                # KMeans + HCA, N=43 oficial + N=18 complementario
│   ├── q5_population_usage.py          # OLS + Logistic, 3 Y Anthropic-based (NUEVO v0.2)
│   ├── q6_public_sector.py             # OLS + Logistic, 5 Y primarias + 1 aux (NUEVO v0.2)
│   └── run_all.py                      # orquestador: q4 → q1 → q3 → q2 → q5 → q6 → manifest
├── tests/
│   ├── conftest.py
│   ├── test_bundle_integrity.py        # hashes SHA-256 del bundle (incluye expansión v0.2)
│   ├── test_split_integrity.py         # 34/9 disjoint, CHL en train
│   ├── test_q1_investment.py
│   ├── test_q2_adoption.py
│   ├── test_q3_innovation.py
│   ├── test_q4_clustering.py
│   ├── test_q5_population_usage.py     # NUEVO v0.2
│   ├── test_q6_public_sector.py        # NUEVO v0.2
│   └── test_outputs_no_causal_language.py
├── notebooks/
│   └── 06_modeling.ipynb               # verificación visual cell-by-cell
└── outputs/
    ├── q1_results.csv                  # long-format por (y_var, model, x_var)
    ├── q1_consistency.csv              # tabla de consistencia de signo
    ├── q1_psm_matched_pairs.csv        # pares PSM (status="ok"|"exploratory"|"insufficient_n")
    ├── q1_psm_balance_diagnostics.csv  # SMD pre/post matching
    ├── q2_results.csv                  # Logit + RF coefs/importance
    ├── q2_predictions_per_country.csv  # P(alta_adopción) por los 43 países
    ├── q3_results.csv                  # primary + auxiliary tier
    ├── q3_consistency.csv              # análogo a q1
    ├── q4_clusters.csv                 # cluster_n43_kmeans/hca + cluster_n18_kmeans/hca
    ├── q4_distance_matrix_n43.csv      # long-format triplete
    ├── q4_distance_matrix_n18.csv      # long-format triplete
    ├── q4_silhouette_scores.csv        # 4 filas mín
    ├── q4_centroids.csv                # centroide por cluster (para Chile deep-dive)
    ├── q5_results.csv                  # OLS + Logistic, 3 Y Anthropic+Oxford (NUEVO v0.2)
    ├── q5_consistency.csv              # consistency table de signo (NUEVO v0.2)
    ├── q5_predictions_per_country.csv  # P(alto_uso_población) por país (NUEVO v0.2)
    ├── q6_results.csv                  # OLS + Logistic, 5 Y primarias + 1 aux (NUEVO v0.2)
    ├── q6_consistency.csv              # consistency table primary tier (NUEVO v0.2)
    ├── q6_predictions_per_country.csv  # P(alto_uso_sector_público) por país (NUEVO v0.2)
    └── fase6_manifest.json             # git_sha + hashes outputs + decisions_log

# NOTA v0.4: NO se generan archivos pca_* (Decisión L). Análisis exploratorio de
# varianza/redundancia delegado a Fase 4 (eda_redundancy_report.csv,
# eda_inter_block_correlations.csv, eda_candidates_for_feature_engineering.csv).
```

**20 outputs en `FASE6/outputs/` (19 CSV + 1 JSON manifest).**

---

## 5. Especificación módulo por módulo

### 5.1 `config/fase6_models.yaml`

```yaml
version: "0.1"
fecha: "2026-05-07"

ridge:
  alpha: 1.0
  random_state: 42

lasso:
  alpha: 0.1
  max_iter: 10000
  random_state: 42

random_forest:
  n_estimators: 300
  max_depth: 4
  min_samples_leaf: 3
  random_state: 42

gradient_boosting:
  n_estimators: 200
  max_depth: 3
  learning_rate: 0.05
  random_state: 42

logistic:
  penalty: l2
  C: 1.0
  max_iter: 1000
  class_weight: balanced
  random_state: 42

bootstrap:
  n_iterations: 2000
  ci_method: BCa
  random_state: 42

cv:
  k_fold: 5
  n_repeats: 10
  stratify_by: region

fdr:
  method: benjamini_hochberg
  alpha: 0.05
```

### 5.2 `config/fase6_decisions.yaml`

```yaml
version: "0.1"
fecha: "2026-05-07"
firma_humana: PENDIENTE

decisiones:
  - id: F6-A
    decision: "Orden ejecución: Q4 → Q1 → Q3 → Q2 → PSM auxiliar"
    justificacion: "Q4 self-contained desbloquea narrativa; Q1 consolida _common_regression; Q3 reusa; Q2 último por binarización"
    estado: approved

  - id: F6-B
    decision: "Bootstrap-OLS como motor principal de Q1; PSM degradado a exploratorio (caliper 0.20)"
    justificacion: "6 tratados + 12 controles producen 3-5 pares — IC PSM no interpretables. Bootstrap 2000 sobre OLS da IC válidos"
    estado: approved

  - id: F6-C
    decision: "Q3 dual: primary (6 Y sin Stanford) + auxiliary (solo Stanford con N=26)"
    justificacion: "C1 pierde info; C2 contamina promedio signos; C3 honesto"
    estado: approved

  - id: F6-D
    decision: "Repeated 5-fold CV + Bootstrap 2000 + LOOCV sanity check"
    justificacion: "5-fold para R²/AUC; bootstrap para IC coeficientes; LOOCV para detectar inestabilidad"
    estado: approved

  - id: F6-E
    decision: "Q1 modelos separados por Y + tabla consistency + FDR-BH"
    justificacion: "Audiencia política necesita 'efecto sobre {algo concreto}'. Índice compuesto opaca. SUR requiere pareo"
    estado: approved

  - id: F6-F
    decision: "Hyperparams fijos en producción; GridSearchCV solo en Fase 7"
    justificacion: "Con N=18-25 GridSearch infla métricas y hyperparams artificiales. F1 elimina investigator degrees of freedom"
    estado: approved

  - id: F6-G
    decision: "Q4 dual: cluster N=43 oficial (Gower mixto) + N=18 complementario (Jaccard binario puro)"
    justificacion: "False clustering por missingness si forzamos N=43 con vector binario IAPP. Dual es honesto"
    estado: approved

  - id: F6-H
    decision: "Train/test split aceptado tal cual; agregar test_split_integrity.py"
    justificacion: "Anomalía reportada fue parsing artifact, no bug real. CSV correcto verificado"
    estado: approved

  - id: F6-I
    decision: "Lenguaje 'asociación' por defecto; 'ATT' solo en sección PSM con supuestos explícitos"
    justificacion: "Asimetría política: overclaim daña credibilidad permanentemente"
    estado: approved

  - id: F6-J
    decision: "Output schemas long-format, una fila por (y_var, model, x_var)"
    justificacion: "Fase 8 hace .query() sin reshape; auditable celda-a-celda; agregar Y/modelos no rompe schema"
    estado: approved
```

### 5.3 `src/_common_data.py`

```python
"""Carga y validación de bundle phase6_ready desde Fase 5."""

from __future__ import annotations
import hashlib
import json
import sys
from functools import lru_cache
from pathlib import Path

import pandas as pd
import yaml

PROJECT_ROOT = Path(__file__).resolve().parents[3].parent
F5_F8_MVP = PROJECT_ROOT / "F5_F8_MVP"
FASE5_BUNDLE = F5_F8_MVP / "FASE5" / "outputs" / "phase6_ready"
FASE5_SRC = F5_F8_MVP / "FASE5"

# Asegurar import de FASE5.src.api
if str(FASE5_SRC) not in sys.path:
    sys.path.insert(0, str(FASE5_SRC))


def sha256_file(path: Path) -> str:
    h = hashlib.sha256()
    with open(path, "rb") as f:
        for chunk in iter(lambda: f.read(65536), b""):
            h.update(chunk)
    return h.hexdigest()


def validate_bundle_hash() -> dict:
    """Valida que cada archivo del bundle reproduce su sha256 según manifest."""
    with open(FASE5_BUNDLE / "phase6_ready_manifest.json") as f:
        manifest = json.load(f)
    errors = []
    for fname, meta in manifest.get("files", {}).items():
        p = FASE5_BUNDLE / fname
        if not p.exists():
            errors.append(f"missing: {fname}")
            continue
        actual = sha256_file(p)
        if actual != meta["sha256"]:
            errors.append(f"sha mismatch: {fname}")
    if errors:
        raise RuntimeError(f"Bundle integrity FAIL: {errors}")
    return {"status": "ok", "n_files": len(manifest["files"])}


@lru_cache(maxsize=1)
def load_bundle() -> dict:
    """Carga todos los componentes del bundle en un dict."""
    validate_bundle_hash()
    from src.api import (  # FASE5.src.api
        load_phase6_feature_matrix,
        load_phase6_schema,
        load_phase6_column_groups,
        load_phase6_modeling_contract,
        load_phase6_ready_manifest,
        load_phase6_llm_context,
    )
    return {
        "feature_matrix": load_phase6_feature_matrix(),
        "schema": load_phase6_schema(),
        "column_groups": load_phase6_column_groups(),
        "contract": load_phase6_modeling_contract(),
        "manifest": load_phase6_ready_manifest(),
        "llm_context": load_phase6_llm_context(),
    }


def get_train_test_split() -> pd.DataFrame:
    return pd.read_csv(FASE5_BUNDLE / "phase6_train_test_split.csv")


def get_x2_controls() -> list[str]:
    """12 controles X2 según contrato."""
    bundle = load_bundle()
    return bundle["contract"]["variables_by_role"]["X2_control"]


def get_x1_aggregated() -> list[str]:
    """Variables agregadas regulatorias (0% missing en 43 países)."""
    return ["n_binding", "n_non_binding", "n_hybrid", "regulatory_intensity", "n_regulatory_mechanisms"]


def get_x1_iapp_raw() -> list[str]:
    """6 variables IAPP raw (cobertura 18/43)."""
    return [
        "iapp_ley_ia_vigente", "iapp_categoria_obligatoriedad",
        "iapp_proyecto_ley_ia", "iapp_modelo_gobernanza",
        "iapp_n_leyes_relacionadas", "iapp_n_autoridades",
    ]


def get_y_by_question(q: str) -> list[str]:
    """Y candidates por sub-pregunta. q ∈ {'Q1','Q2','Q3'}."""
    bundle = load_bundle()
    q_to_role = {"Q1": "Y_Q1_investment", "Q2": "Y_Q2_adoption", "Q3": "Y_Q3_innovation"}
    return bundle["contract"]["variables_by_role"][q_to_role[q]]
```

### 5.4 `src/_common_regression.py`

```python
"""Utilidades de regresión: OLS, Ridge, Lasso, bootstrap, FDR."""

from __future__ import annotations
import warnings

import numpy as np
import pandas as pd
import statsmodels.api as sm
from sklearn.linear_model import Ridge, Lasso
from sklearn.model_selection import (
    cross_val_score, RepeatedKFold, LeaveOneOut, KFold
)


def fdr_benjamini_hochberg(pvalues: np.ndarray, alpha: float = 0.05) -> np.ndarray:
    """BH FDR correction. Returns adjusted p-values."""
    n = len(pvalues)
    if n == 0:
        return pvalues
    pvalues = np.nan_to_num(pvalues.astype(float), nan=1.0, posinf=1.0, neginf=1.0)
    order = np.argsort(pvalues)
    ranked = np.empty(n)
    ranked[order] = np.arange(1, n + 1)
    adjusted = np.minimum(1.0, pvalues * n / ranked)
    for i in range(n - 2, -1, -1):
        adjusted[order[i]] = min(adjusted[order[i]], adjusted[order[i + 1]])
    return adjusted


def fit_ols(df: pd.DataFrame, y: str, x_cols: list[str]) -> dict:
    """OLS con statsmodels. Retorna coeficientes, IC asintóticos, p-values."""
    sub = df[[y] + x_cols].dropna()
    if len(sub) < 10:
        return {"status": "insufficient_n", "n": len(sub)}
    X = sm.add_constant(sub[x_cols])
    model = sm.OLS(sub[y], X).fit()
    ci = model.conf_int(0.05)
    return {
        "status": "ok",
        "n": len(sub),
        "r2": float(model.rsquared),
        "r2_adj": float(model.rsquared_adj),
        "coefficients": model.params.to_dict(),
        "std_errors": model.bse.to_dict(),
        "pvalues": model.pvalues.to_dict(),
        "ci95_lower": ci[0].to_dict(),
        "ci95_upper": ci[1].to_dict(),
    }


def fit_ols_with_bootstrap(
    df: pd.DataFrame, y: str, x_cols: list[str],
    n_boot: int = 2000, seed: int = 42,
) -> dict:
    """OLS + bootstrap (B=2000) para IC95 BCa de cada coeficiente."""
    sub = df[[y] + x_cols].dropna()
    if len(sub) < 15:
        return {"status": "insufficient_n", "n": len(sub)}

    rng = np.random.default_rng(seed)
    point = sm.OLS(sub[y], sm.add_constant(sub[x_cols])).fit()
    point_coefs = point.params

    boot_coefs = {col: [] for col in point_coefs.index}
    for _ in range(n_boot):
        idx = rng.integers(0, len(sub), len(sub))
        try:
            with warnings.catch_warnings():
                warnings.simplefilter("ignore")
                b = sm.OLS(
                    sub[y].iloc[idx],
                    sm.add_constant(sub[x_cols].iloc[idx]),
                ).fit()
            for col in point_coefs.index:
                boot_coefs[col].append(b.params.get(col, np.nan))
        except Exception:
            continue

    results = {}
    for col, vals in boot_coefs.items():
        arr = np.array(vals)
        arr = arr[np.isfinite(arr)]
        if len(arr) < 100:
            results[col] = {
                "ci95_lower": np.nan, "ci95_upper": np.nan,
                "p_two_sided_bootstrap": np.nan,
                "n_boot_valid": int(len(arr)),
            }
            continue
        results[col] = {
            "ci95_lower": float(np.nanpercentile(arr, 2.5)),
            "ci95_upper": float(np.nanpercentile(arr, 97.5)),
            "p_two_sided_bootstrap": float(2 * min(
                (arr <= 0).mean(), (arr >= 0).mean(),
            )),
            "n_boot_valid": int(len(arr)),
        }

    return {
        "status": "ok",
        "n": len(sub),
        "r2": float(point.rsquared),
        "r2_adj": float(point.rsquared_adj),
        "coefficients": point_coefs.to_dict(),
        "std_errors_asymptotic": point.bse.to_dict(),
        "pvalues_asymptotic": point.pvalues.to_dict(),
        "bootstrap": results,
    }


def fit_ridge_lasso_cv(
    df: pd.DataFrame, y: str, x_cols: list[str],
    alpha_ridge: float = 1.0, alpha_lasso: float = 0.1,
    cv_repeats: int = 10, cv_splits: int = 5, seed: int = 42,
) -> dict:
    """Ridge y Lasso con Repeated K-fold CV."""
    sub = df[[y] + x_cols].dropna()
    if len(sub) < 15:
        return {"status": "insufficient_n", "n": len(sub)}
    X = sub[x_cols].values
    ytrue = sub[y].values
    cv = RepeatedKFold(n_splits=cv_splits, n_repeats=cv_repeats, random_state=seed)

    ridge = Ridge(alpha=alpha_ridge, random_state=seed)
    lasso = Lasso(alpha=alpha_lasso, max_iter=10000, random_state=seed)

    ridge_cv = cross_val_score(ridge, X, ytrue, cv=cv, scoring="r2")
    lasso_cv = cross_val_score(lasso, X, ytrue, cv=cv, scoring="r2")

    ridge_full = Ridge(alpha=alpha_ridge, random_state=seed).fit(X, ytrue)
    lasso_full = Lasso(alpha=alpha_lasso, max_iter=10000, random_state=seed).fit(X, ytrue)

    # LOOCV sanity check
    loo = LeaveOneOut()
    ridge_loo = cross_val_score(ridge, X, ytrue, cv=loo, scoring="r2").mean()
    lasso_loo = cross_val_score(lasso, X, ytrue, cv=loo, scoring="r2").mean()

    return {
        "status": "ok",
        "n": len(sub),
        "ridge_cv_r2_mean": float(ridge_cv.mean()),
        "ridge_cv_r2_std": float(ridge_cv.std()),
        "ridge_loo_r2": float(ridge_loo),
        "ridge_coef": dict(zip(x_cols, ridge_full.coef_.tolist())),
        "ridge_intercept": float(ridge_full.intercept_),
        "lasso_cv_r2_mean": float(lasso_cv.mean()),
        "lasso_cv_r2_std": float(lasso_cv.std()),
        "lasso_loo_r2": float(lasso_loo),
        "lasso_coef": dict(zip(x_cols, lasso_full.coef_.tolist())),
        "lasso_intercept": float(lasso_full.intercept_),
    }
```

### 5.5 `src/_common_classification.py`

```python
"""Utilidades de clasificación: binarización, Logistic, Random Forest."""

from __future__ import annotations
import numpy as np
import pandas as pd
from sklearn.linear_model import LogisticRegression
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import (
    cross_val_score, RepeatedKFold, LeaveOneOut, cross_val_predict,
)
from sklearn.metrics import (
    roc_auc_score, confusion_matrix, classification_report,
)


def binarize_by_median(s: pd.Series) -> tuple[pd.Series, float]:
    """Y_binary = 1 si país está sobre la mediana."""
    median = float(s.median())
    return (s >= median).astype(int), median


def fit_logistic_cv(
    df: pd.DataFrame, y_binary: str, x_cols: list[str],
    cv_repeats: int = 10, cv_splits: int = 5, seed: int = 42,
) -> dict:
    """Logistic regression con CV repetida + LOOCV sanity."""
    sub = df[[y_binary] + x_cols].dropna()
    if len(sub) < 20:
        return {"status": "insufficient_n", "n": len(sub)}
    X = sub[x_cols].values
    y = sub[y_binary].values

    cv = RepeatedKFold(n_splits=cv_splits, n_repeats=cv_repeats, random_state=seed)
    logreg = LogisticRegression(
        penalty="l2", C=1.0, max_iter=1000,
        class_weight="balanced", random_state=seed,
    )
    auc_cv = cross_val_score(logreg, X, y, cv=cv, scoring="roc_auc")

    loo = LeaveOneOut()
    auc_loo = cross_val_score(logreg, X, y, cv=loo, scoring="roc_auc").mean()

    full = LogisticRegression(
        penalty="l2", C=1.0, max_iter=1000,
        class_weight="balanced", random_state=seed,
    ).fit(X, y)

    y_pred = cross_val_predict(logreg, X, y, cv=cv, method="predict")
    cm = confusion_matrix(y, y_pred)

    return {
        "status": "ok",
        "n": len(sub),
        "n_class_0": int((y == 0).sum()),
        "n_class_1": int((y == 1).sum()),
        "auc_cv_5fold_mean": float(auc_cv.mean()),
        "auc_cv_5fold_std": float(auc_cv.std()),
        "auc_loocv": float(auc_loo),
        "logistic_coef": dict(zip(x_cols, full.coef_[0].tolist())),
        "logistic_intercept": float(full.intercept_[0]),
        "confusion_tn": int(cm[0, 0]),
        "confusion_fp": int(cm[0, 1]),
        "confusion_fn": int(cm[1, 0]),
        "confusion_tp": int(cm[1, 1]),
    }


def fit_random_forest_cv(
    df: pd.DataFrame, y_binary: str, x_cols: list[str],
    n_estimators: int = 300, max_depth: int = 4,
    min_samples_leaf: int = 3, seed: int = 42,
    cv_repeats: int = 10, cv_splits: int = 5,
) -> dict:
    """Random Forest con CV repetida + feature importance."""
    sub = df[[y_binary] + x_cols].dropna()
    if len(sub) < 20:
        return {"status": "insufficient_n", "n": len(sub)}
    X = sub[x_cols].values
    y = sub[y_binary].values

    cv = RepeatedKFold(n_splits=cv_splits, n_repeats=cv_repeats, random_state=seed)
    rf = RandomForestClassifier(
        n_estimators=n_estimators, max_depth=max_depth,
        min_samples_leaf=min_samples_leaf, random_state=seed,
    )
    auc_cv = cross_val_score(rf, X, y, cv=cv, scoring="roc_auc")

    loo = LeaveOneOut()
    auc_loo = cross_val_score(rf, X, y, cv=loo, scoring="roc_auc").mean()

    full = RandomForestClassifier(
        n_estimators=n_estimators, max_depth=max_depth,
        min_samples_leaf=min_samples_leaf, random_state=seed,
    ).fit(X, y)

    return {
        "status": "ok",
        "n": len(sub),
        "auc_cv_5fold_mean": float(auc_cv.mean()),
        "auc_cv_5fold_std": float(auc_cv.std()),
        "auc_loocv": float(auc_loo),
        "feature_importance": dict(zip(x_cols, full.feature_importances_.tolist())),
    }
```

### 5.6 `src/q4_clustering.py`

```python
"""Q4: Clustering de países por perfil regulatorio.
Doble clustering: N=43 oficial (Gower) + N=18 complementario (Jaccard binario)."""

from __future__ import annotations
import numpy as np
import pandas as pd
from scipy.cluster.hierarchy import linkage, fcluster
from scipy.spatial.distance import squareform
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score
from sklearn.preprocessing import StandardScaler

from ._common_data import load_bundle, F5_F8_MVP

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"


def get_features_n43(fm: pd.DataFrame) -> tuple[pd.DataFrame, list[str]]:
    """Features mixtas para clustering N=43: 9 one-hots + 5 agregados."""
    one_hots = [c for c in fm.columns if (
        c.startswith("iapp_categoria_obligatoriedad_") or
        c.startswith("iapp_modelo_gobernanza_")
    )]
    aggregates = ["n_binding", "n_non_binding", "n_hybrid",
                  "regulatory_intensity", "n_regulatory_mechanisms"]
    cols = one_hots + aggregates
    cols = [c for c in cols if c in fm.columns]
    sub = fm[["iso3"] + cols].copy()
    return sub, cols


def get_features_n18(fm: pd.DataFrame) -> tuple[pd.DataFrame, list[str]]:
    """Vector binario IAPP para subset de 18 con datos."""
    iapp_binary_cols = [
        "iapp_ley_ia_vigente", "iapp_proyecto_ley_ia",
    ]
    one_hots = [c for c in fm.columns if (
        c.startswith("iapp_categoria_obligatoriedad_") or
        c.startswith("iapp_modelo_gobernanza_")
    )]
    cols = iapp_binary_cols + one_hots
    cols = [c for c in cols if c in fm.columns]
    sub = fm[fm["iapp_ley_ia_vigente"].notna()][["iso3"] + cols].copy()
    return sub, cols


def jaccard_distance_matrix(X: np.ndarray) -> np.ndarray:
    """Jaccard distance entre filas (binario)."""
    n = X.shape[0]
    D = np.zeros((n, n))
    for i in range(n):
        for j in range(i + 1, n):
            inter = np.sum(np.logical_and(X[i] == 1, X[j] == 1))
            union = np.sum(np.logical_or(X[i] == 1, X[j] == 1))
            d = 1 - inter / union if union > 0 else 0.0
            D[i, j] = D[j, i] = d
    return D


def euclidean_standardized_distance(X: np.ndarray) -> np.ndarray:
    """Euclidean sobre features estandarizadas (z-score)."""
    Xs = StandardScaler().fit_transform(X)
    n = Xs.shape[0]
    D = np.zeros((n, n))
    for i in range(n):
        for j in range(i + 1, n):
            d = float(np.linalg.norm(Xs[i] - Xs[j]))
            D[i, j] = D[j, i] = d
    return D


def hca_clusters(D: np.ndarray, k: int = 4) -> np.ndarray:
    cond = squareform(D, checks=False)
    Z = linkage(cond, method="ward")
    return fcluster(Z, t=k, criterion="maxclust")


def kmeans_clusters(X: np.ndarray, k: int = 4, seed: int = 42) -> np.ndarray:
    return KMeans(n_clusters=k, random_state=seed, n_init=20).fit_predict(X)


def run_q4(seed: int = 42, k: int = 4) -> dict:
    bundle = load_bundle()
    fm = bundle["feature_matrix"]

    # ===== N=43 OFICIAL =====
    sub43, cols43 = get_features_n43(fm)
    X43 = sub43[cols43].values.astype(float)
    D43 = euclidean_standardized_distance(X43)
    hca43 = hca_clusters(D43, k=k)
    km43 = kmeans_clusters(StandardScaler().fit_transform(X43), k=k, seed=seed)
    sil_hca43 = silhouette_score(D43, hca43, metric="precomputed") if len(set(hca43)) > 1 else np.nan
    sil_km43 = silhouette_score(StandardScaler().fit_transform(X43), km43) if len(set(km43)) > 1 else np.nan

    # ===== N=18 COMPLEMENTARIO =====
    sub18, cols18 = get_features_n18(fm)
    X18 = sub18[cols18].fillna(0).astype(int).values
    D18 = jaccard_distance_matrix(X18)
    hca18 = hca_clusters(D18, k=min(k, max(2, X18.shape[0] // 4)))
    km18 = kmeans_clusters(X18.astype(float), k=min(k, max(2, X18.shape[0] // 4)), seed=seed)
    sil_hca18 = silhouette_score(D18, hca18, metric="precomputed") if len(set(hca18)) > 1 else np.nan
    sil_km18 = silhouette_score(X18.astype(float), km18) if len(set(km18)) > 1 else np.nan

    # ===== ENSAMBLAR clusters.csv =====
    clusters_df = fm[["iso3", "country_name_canonical", "region"]].copy()
    clusters_df["cluster_kmeans_n43"] = km43
    clusters_df["cluster_hca_n43"] = hca43
    clusters_df["in_iapp_subset"] = clusters_df["iso3"].isin(sub18["iso3"]).astype(int)
    clusters_df["cluster_kmeans_n18"] = np.nan
    clusters_df["cluster_hca_n18"] = np.nan
    iso_to_km18 = dict(zip(sub18["iso3"], km18))
    iso_to_hca18 = dict(zip(sub18["iso3"], hca18))
    clusters_df["cluster_kmeans_n18"] = clusters_df["iso3"].map(iso_to_km18)
    clusters_df["cluster_hca_n18"] = clusters_df["iso3"].map(iso_to_hca18)

    OUTPUTS.mkdir(parents=True, exist_ok=True)
    clusters_df.to_csv(OUTPUTS / "q4_clusters.csv", index=False)

    # Distance matrices long-format
    def matrix_to_long(D, isos, metric, scope):
        rows = []
        for i, a in enumerate(isos):
            for j, b in enumerate(isos):
                if i < j:
                    rows.append({
                        "iso3_a": a, "iso3_b": b,
                        "distance": float(D[i, j]),
                        "distance_metric": metric, "scope": scope,
                    })
        return pd.DataFrame(rows)

    matrix_to_long(D43, sub43["iso3"].tolist(), "euclidean_standardized", "n43_mixed").to_csv(
        OUTPUTS / "q4_distance_matrix_n43.csv", index=False)
    matrix_to_long(D18, sub18["iso3"].tolist(), "jaccard", "n18_binary").to_csv(
        OUTPUTS / "q4_distance_matrix_n18.csv", index=False)

    # Silhouette
    pd.DataFrame([
        {"method": "kmeans", "scope": "n43_mixed", "k": k, "silhouette_score": sil_km43, "n_observations": 43},
        {"method": "hca",    "scope": "n43_mixed", "k": k, "silhouette_score": sil_hca43, "n_observations": 43},
        {"method": "kmeans", "scope": "n18_binary", "k": min(k, max(2, X18.shape[0]//4)), "silhouette_score": sil_km18, "n_observations": 18},
        {"method": "hca",    "scope": "n18_binary", "k": min(k, max(2, X18.shape[0]//4)), "silhouette_score": sil_hca18, "n_observations": 18},
    ]).to_csv(OUTPUTS / "q4_silhouette_scores.csv", index=False)

    # Centroides KMeans n43 (para deep-dive Chile)
    centroids = pd.DataFrame(
        StandardScaler().fit(X43).inverse_transform(
            KMeans(n_clusters=k, random_state=seed, n_init=20)
            .fit(StandardScaler().fit_transform(X43)).cluster_centers_
        ),
        columns=cols43,
    )
    centroids.insert(0, "cluster_label", range(k))
    centroids.to_csv(OUTPUTS / "q4_centroids.csv", index=False)

    return {
        "n43_kmeans_silhouette": sil_km43,
        "n43_hca_silhouette": sil_hca43,
        "n18_kmeans_silhouette": sil_km18,
        "n18_hca_silhouette": sil_hca18,
        "k": k,
    }
```

### 5.7 `src/q1_investment.py`

```python
"""Q1 Inversión: OLS + Ridge + Lasso + bootstrap + PSM exploratorio."""

from __future__ import annotations
import numpy as np
import pandas as pd
from sklearn.linear_model import LogisticRegression

from ._common_data import (
    load_bundle, get_train_test_split, get_x2_controls, get_x1_aggregated,
    get_x1_iapp_raw, F5_F8_MVP,
)
from ._common_regression import (
    fit_ols, fit_ols_with_bootstrap, fit_ridge_lasso_cv, fdr_benjamini_hochberg,
)

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"

Y_VARS_Q1 = [
    "oxford_ind_company_investment_emerging_tech",
    "oxford_ind_ai_unicorns_log",
    "oxford_ind_non_ai_unicorns_log",
    "oxford_ind_vc_availability",
    "wipo_c_vencapdeal_score",
    "wb_fdi_net_inflows",  # Y_Q1_or_control
]


def run_q1(seed: int = 42, n_boot: int = 2000) -> dict:
    bundle = load_bundle()
    fm = bundle["feature_matrix"]
    x1_agg = get_x1_aggregated()
    x2 = get_x2_controls()

    # Usar versiones _z donde estén disponibles, sino raw
    def col_z(c):
        return f"{c}_z" if f"{c}_z" in fm.columns else c
    x1_agg_z = [col_z(c) for c in x1_agg]
    x2_z = [col_z(c) for c in x2]

    rows_results = []
    rows_consistency = []

    for y in Y_VARS_Q1:
        if y not in fm.columns:
            continue

        # === Modelo solo controles X2 ===
        ols_x2 = fit_ols(fm, y, x2_z)
        # === Modelo X1 agregadas + X2 ===
        ols_full = fit_ols_with_bootstrap(fm, y, x1_agg_z + x2_z, n_boot=n_boot, seed=seed)
        # === Ridge + Lasso ===
        rl = fit_ridge_lasso_cv(fm, y, x1_agg_z + x2_z, seed=seed)

        # Persistir filas long
        if ols_full.get("status") == "ok":
            for x in x1_agg_z + x2_z + ["const"]:
                rows_results.append({
                    "question": "Q1", "y_var": y, "model": "OLS_full",
                    "x_var": x,
                    "coefficient": ols_full["coefficients"].get(x),
                    "std_error": ols_full["std_errors_asymptotic"].get(x),
                    "p_value": ols_full["pvalues_asymptotic"].get(x),
                    "ci95_lower": ols_full["bootstrap"].get(x, {}).get("ci95_lower"),
                    "ci95_upper": ols_full["bootstrap"].get(x, {}).get("ci95_upper"),
                    "p_value_bootstrap": ols_full["bootstrap"].get(x, {}).get("p_two_sided_bootstrap"),
                    "n_effective": ols_full["n"],
                    "r2": ols_full["r2"],
                    "ci_method": "bootstrap_2000",
                    "n_boot": n_boot,
                    "seed": seed,
                })

        if ols_x2.get("status") == "ok":
            rows_results.append({
                "question": "Q1", "y_var": y, "model": "OLS_only_X2",
                "x_var": "__model_metadata__",
                "n_effective": ols_x2["n"],
                "r2": ols_x2["r2"], "r2_adj": ols_x2["r2_adj"],
                "seed": seed,
            })

        if rl.get("status") == "ok":
            for x, coef in rl["ridge_coef"].items():
                rows_results.append({
                    "question": "Q1", "y_var": y, "model": "Ridge",
                    "x_var": x, "coefficient": coef,
                    "n_effective": rl["n"],
                    "r2": rl["ridge_cv_r2_mean"],
                    "cv_r2_mean_5fold": rl["ridge_cv_r2_mean"],
                    "cv_r2_std_5fold": rl["ridge_cv_r2_std"],
                    "loocv_r2": rl["ridge_loo_r2"],
                    "model_hyperparams_json": '{"alpha":1.0}',
                    "seed": seed,
                })
            for x, coef in rl["lasso_coef"].items():
                rows_results.append({
                    "question": "Q1", "y_var": y, "model": "Lasso",
                    "x_var": x, "coefficient": coef,
                    "n_effective": rl["n"],
                    "r2": rl["lasso_cv_r2_mean"],
                    "cv_r2_mean_5fold": rl["lasso_cv_r2_mean"],
                    "cv_r2_std_5fold": rl["lasso_cv_r2_std"],
                    "loocv_r2": rl["lasso_loo_r2"],
                    "model_hyperparams_json": '{"alpha":0.1}',
                    "seed": seed,
                })

    df = pd.DataFrame(rows_results)
    OUTPUTS.mkdir(parents=True, exist_ok=True)

    # FDR sobre p-values del X1 focal (n_binding) cruzando los 5-6 Y
    focal = "n_binding_z" if "n_binding_z" in fm.columns else "n_binding"
    focal_rows = df[(df["x_var"] == focal) & (df["model"] == "OLS_full") & df["p_value"].notna()]
    if len(focal_rows) > 0:
        pvals = focal_rows["p_value"].values
        fdr_q = fdr_benjamini_hochberg(pvals)
        df.loc[focal_rows.index, "fdr_q_value"] = fdr_q
        df.loc[focal_rows.index, "fdr_significant_05"] = (fdr_q < 0.05)

    df.to_csv(OUTPUTS / "q1_results.csv", index=False)

    # === Tabla consistency ===
    for x in x1_agg_z:
        x_rows = df[(df["x_var"] == x) & (df["model"] == "OLS_full")]
        n_total = len(x_rows)
        if n_total == 0:
            continue
        n_neg = (x_rows["coefficient"] < 0).sum()
        n_pos = (x_rows["coefficient"] > 0).sum()
        n_sig_05 = (x_rows["p_value"] < 0.05).sum()
        n_sig_fdr = (x_rows.get("fdr_significant_05", pd.Series(False)) == True).sum()
        if n_neg >= max(2, 0.66 * n_total) and n_sig_05 >= 1:
            direction = "robust_negative"
        elif n_pos >= max(2, 0.66 * n_total) and n_sig_05 >= 1:
            direction = "robust_positive"
        elif n_sig_05 == 0:
            direction = "null"
        else:
            direction = "mixed"
        rows_consistency.append({
            "question": "Q1", "x_var": x,
            "n_y_total": n_total, "n_y_negative": int(n_neg), "n_y_positive": int(n_pos),
            "n_y_significant_05": int(n_sig_05),
            "n_y_significant_05_after_fdr": int(n_sig_fdr),
            "direction_summary": direction,
        })
    pd.DataFrame(rows_consistency).to_csv(OUTPUTS / "q1_consistency.csv", index=False)

    # === PSM EXPLORATORIO ===
    psm_result = propensity_score_match_exploratory(fm, treatment_col="iapp_ley_ia_vigente", caliper=0.20)
    psm_result.to_csv(OUTPUTS / "q1_psm_matched_pairs.csv", index=False)

    return {"n_rows_results": len(df), "n_rows_consistency": len(rows_consistency)}


def propensity_score_match_exploratory(
    fm: pd.DataFrame, treatment_col: str = "iapp_ley_ia_vigente",
    caliper: float = 0.20, seed: int = 42,
) -> pd.DataFrame:
    """PSM 1:1 con caliper amplio. Marcado como exploratorio."""
    x2 = get_x2_controls()
    sub = fm[[treatment_col, "iso3"] + x2].dropna()
    if sub[treatment_col].sum() < 3 or (1 - sub[treatment_col]).sum() < 3:
        return pd.DataFrame([{"status": "insufficient_n"}])

    X = sub[x2].values
    T = sub[treatment_col].astype(int).values

    ps_model = LogisticRegression(max_iter=1000).fit(X, T)
    ps = ps_model.predict_proba(X)[:, 1]
    sub = sub.assign(propensity=ps)

    treated = sub[sub[treatment_col] == 1].sort_values("propensity")
    control = sub[sub[treatment_col] == 0].sort_values("propensity")

    matches = []
    used_idx = set()
    for tidx, trow in treated.iterrows():
        candidates = control[~control.index.isin(used_idx)]
        if candidates.empty:
            break
        diffs = (candidates["propensity"] - trow["propensity"]).abs()
        best = diffs.idxmin()
        if diffs[best] <= caliper:
            matches.append({
                "treated_iso3": trow["iso3"],
                "control_iso3": candidates.loc[best, "iso3"],
                "propensity_treated": float(trow["propensity"]),
                "propensity_control": float(candidates.loc[best, "propensity"]),
                "ps_diff": float(diffs[best]),
                "caliper": caliper,
                "treatment_col": treatment_col,
                "status": "ok",
            })
            used_idx.add(best)

    if len(matches) < 5:
        for m in matches:
            m["status"] = "exploratory_low_n"
    if len(matches) == 0:
        return pd.DataFrame([{"status": "insufficient_n", "caliper": caliper, "treatment_col": treatment_col}])
    return pd.DataFrame(matches)
```

### 5.8 `src/q3_innovation.py`

```python
"""Q3 Innovación: Ridge + Gradient Boosting, primary vs auxiliary tier."""

from __future__ import annotations
import pandas as pd
from sklearn.ensemble import GradientBoostingRegressor
from sklearn.linear_model import Ridge
from sklearn.model_selection import cross_val_score, RepeatedKFold, LeaveOneOut

from ._common_data import (
    load_bundle, get_x2_controls, get_x1_aggregated, F5_F8_MVP,
)
from ._common_regression import (
    fit_ols_with_bootstrap, fit_ridge_lasso_cv, fdr_benjamini_hochberg,
)

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"

Y_VARS_Q3_PRIMARY = [
    "wipo_out_score",
    "oxford_total_score",
    "wipo_gii_score",
    "oxford_innovation_capacity",
    "oxford_ind_ai_research_papers_log",
    "wb_patent_applications_residents",
]
Y_VARS_Q3_AUXILIARY = [
    "stanford_fig_6_3_5_fig_6_3_5_volume_of_publications",
]


def run_q3(seed: int = 42, n_boot: int = 2000) -> dict:
    bundle = load_bundle()
    fm = bundle["feature_matrix"]
    x1_agg = get_x1_aggregated()
    x2 = get_x2_controls()

    def col_z(c):
        return f"{c}_z" if f"{c}_z" in fm.columns else c
    x1_z = [col_z(c) for c in x1_agg]
    x2_z = [col_z(c) for c in x2]

    rows = []

    def fit_and_record(y, tier):
        ols = fit_ols_with_bootstrap(fm, y, x1_z + x2_z, n_boot=n_boot, seed=seed)
        rl = fit_ridge_lasso_cv(fm, y, x1_z + x2_z, seed=seed)

        # GBR
        sub = fm[[y] + x1_z + x2_z].dropna()
        if len(sub) >= 15:
            X, ytrue = sub[x1_z + x2_z].values, sub[y].values
            cv = RepeatedKFold(n_splits=5, n_repeats=10, random_state=seed)
            gbr = GradientBoostingRegressor(
                n_estimators=200, max_depth=3, learning_rate=0.05, random_state=seed,
            )
            gbr_cv = cross_val_score(gbr, X, ytrue, cv=cv, scoring="r2")
            gbr_loo = cross_val_score(gbr, X, ytrue, cv=LeaveOneOut(), scoring="r2").mean()
            gbr_full = GradientBoostingRegressor(
                n_estimators=200, max_depth=3, learning_rate=0.05, random_state=seed,
            ).fit(X, ytrue)
            for x, imp in zip(x1_z + x2_z, gbr_full.feature_importances_):
                rows.append({
                    "question": "Q3", "tier": tier,
                    "y_var": y, "model": "GBR", "x_var": x,
                    "coefficient_or_importance": float(imp),
                    "n_effective": len(sub),
                    "cv_r2_mean_5fold": float(gbr_cv.mean()),
                    "cv_r2_std_5fold": float(gbr_cv.std()),
                    "loocv_r2": float(gbr_loo),
                    "model_hyperparams_json": '{"n_estimators":200,"max_depth":3,"lr":0.05}',
                    "seed": seed,
                })

        if ols.get("status") == "ok":
            for x in x1_z + x2_z + ["const"]:
                rows.append({
                    "question": "Q3", "tier": tier,
                    "y_var": y, "model": "OLS_full", "x_var": x,
                    "coefficient_or_importance": ols["coefficients"].get(x),
                    "std_error": ols["std_errors_asymptotic"].get(x),
                    "p_value": ols["pvalues_asymptotic"].get(x),
                    "ci95_lower": ols["bootstrap"].get(x, {}).get("ci95_lower"),
                    "ci95_upper": ols["bootstrap"].get(x, {}).get("ci95_upper"),
                    "p_value_bootstrap": ols["bootstrap"].get(x, {}).get("p_two_sided_bootstrap"),
                    "n_effective": ols["n"], "r2": ols["r2"],
                    "ci_method": "bootstrap_2000", "n_boot": n_boot, "seed": seed,
                })

        if rl.get("status") == "ok":
            for x, coef in rl["ridge_coef"].items():
                rows.append({
                    "question": "Q3", "tier": tier,
                    "y_var": y, "model": "Ridge", "x_var": x,
                    "coefficient_or_importance": coef,
                    "n_effective": rl["n"],
                    "cv_r2_mean_5fold": rl["ridge_cv_r2_mean"],
                    "cv_r2_std_5fold": rl["ridge_cv_r2_std"],
                    "loocv_r2": rl["ridge_loo_r2"],
                    "model_hyperparams_json": '{"alpha":1.0}', "seed": seed,
                })

    for y in Y_VARS_Q3_PRIMARY:
        if y in fm.columns:
            fit_and_record(y, tier="primary")
    for y in Y_VARS_Q3_AUXILIARY:
        if y in fm.columns:
            fit_and_record(y, tier="auxiliary_low_n")

    df = pd.DataFrame(rows)

    # FDR sobre n_binding cruzando solo Y primary
    focal = "n_binding_z" if "n_binding_z" in fm.columns else "n_binding"
    primary_focal = df[
        (df["x_var"] == focal) & (df["model"] == "OLS_full") &
        (df["tier"] == "primary") & df["p_value"].notna()
    ]
    if len(primary_focal) > 0:
        pvals = primary_focal["p_value"].values
        fdr_q = fdr_benjamini_hochberg(pvals)
        df.loc[primary_focal.index, "fdr_q_value"] = fdr_q
        df.loc[primary_focal.index, "fdr_significant_05"] = (fdr_q < 0.05)

    df.to_csv(OUTPUTS / "q3_results.csv", index=False)

    # consistency tabla
    cons = []
    for x in x1_z:
        for tier in ["primary", "auxiliary_low_n"]:
            sub = df[(df["x_var"] == x) & (df["model"] == "OLS_full") & (df["tier"] == tier)]
            if len(sub) == 0:
                continue
            n_neg = (sub["coefficient_or_importance"] < 0).sum()
            n_pos = (sub["coefficient_or_importance"] > 0).sum()
            n_sig = (sub["p_value"] < 0.05).sum()
            cons.append({
                "question": "Q3", "tier": tier, "x_var": x,
                "n_y_total": len(sub),
                "n_y_negative": int(n_neg),
                "n_y_positive": int(n_pos),
                "n_y_significant_05": int(n_sig),
            })
    pd.DataFrame(cons).to_csv(OUTPUTS / "q3_consistency.csv", index=False)

    return {"n_rows": len(df)}
```

### 5.9 `src/q2_adoption.py`

```python
"""Q2 Adopción: Logistic + Random Forest sobre Y binarizada."""

from __future__ import annotations
import numpy as np
import pandas as pd
from sklearn.linear_model import LogisticRegression

from ._common_data import (
    load_bundle, get_x2_controls, get_x1_aggregated, F5_F8_MVP,
)
from ._common_classification import (
    binarize_by_median, fit_logistic_cv, fit_random_forest_cv,
)

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"

Y_VARS_Q2 = [
    "ms_h2_2025_ai_diffusion_pct",
    "ms_h1_2025_ai_diffusion_pct",
    "anthropic_usage_pct",
    "anthropic_collaboration_pct",
    "oecd_5_ict_business_oecd_biz_ai_pct",
    "oxford_public_sector_adoption",
    "oxford_ind_adoption_emerging_tech",
]


def run_q2(seed: int = 42) -> dict:
    bundle = load_bundle()
    fm = bundle["feature_matrix"].copy()
    x1_agg = get_x1_aggregated()
    x2 = get_x2_controls()

    def col_z(c):
        return f"{c}_z" if f"{c}_z" in fm.columns else c
    x1_z = [col_z(c) for c in x1_agg]
    x2_z = [col_z(c) for c in x2]

    rows = []
    rows_predictions = []

    for y in Y_VARS_Q2:
        if y not in fm.columns:
            continue
        sub = fm[[y, "iso3"] + x1_z + x2_z].dropna()
        if len(sub) < 20:
            continue
        y_binary, threshold = binarize_by_median(sub[y])
        sub = sub.assign(y_binary=y_binary.values)
        sub_y = sub.copy()

        log = fit_logistic_cv(sub_y, "y_binary", x1_z + x2_z, seed=seed)
        rf = fit_random_forest_cv(sub_y, "y_binary", x1_z + x2_z, seed=seed)

        if log.get("status") == "ok":
            rows.append({
                "question": "Q2", "y_var": y,
                "binarization_threshold_value": float(threshold),
                "binarization_method": "median",
                "model": "Logistic", "x_var": "__model_metadata__",
                "n_effective": log["n"],
                "n_class_0": log["n_class_0"], "n_class_1": log["n_class_1"],
                "auc_cv_5fold_mean": log["auc_cv_5fold_mean"],
                "auc_cv_5fold_std": log["auc_cv_5fold_std"],
                "auc_loocv": log["auc_loocv"],
                "confusion_tp": log["confusion_tp"], "confusion_tn": log["confusion_tn"],
                "confusion_fp": log["confusion_fp"], "confusion_fn": log["confusion_fn"],
                "model_hyperparams_json": '{"penalty":"l2","C":1.0}',
                "seed": seed,
            })
            for x, coef in log["logistic_coef"].items():
                rows.append({
                    "question": "Q2", "y_var": y, "model": "Logistic", "x_var": x,
                    "coefficient_or_importance": coef,
                    "n_effective": log["n"], "seed": seed,
                })

        if rf.get("status") == "ok":
            rows.append({
                "question": "Q2", "y_var": y, "model": "RandomForest",
                "x_var": "__model_metadata__",
                "n_effective": rf["n"],
                "auc_cv_5fold_mean": rf["auc_cv_5fold_mean"],
                "auc_cv_5fold_std": rf["auc_cv_5fold_std"],
                "auc_loocv": rf["auc_loocv"],
                "model_hyperparams_json": '{"n_estimators":300,"max_depth":4}',
                "seed": seed,
            })
            for x, imp in rf["feature_importance"].items():
                rows.append({
                    "question": "Q2", "y_var": y, "model": "RandomForest", "x_var": x,
                    "coefficient_or_importance": imp,
                    "n_effective": rf["n"], "seed": seed,
                })

        # Predicciones por país
        if log.get("status") == "ok":
            X_full = sub_y[x1_z + x2_z].values
            full_log = LogisticRegression(
                penalty="l2", C=1.0, max_iter=1000,
                class_weight="balanced", random_state=seed,
            ).fit(X_full, sub_y["y_binary"].values)
            probs = full_log.predict_proba(X_full)[:, 1]
            for iso3, prob in zip(sub_y["iso3"].values, probs):
                rows_predictions.append({
                    "y_var": y, "iso3": iso3,
                    "p_high_adoption": float(prob),
                    "model": "Logistic_in_sample", "seed": seed,
                })

    pd.DataFrame(rows).to_csv(OUTPUTS / "q2_results.csv", index=False)
    pd.DataFrame(rows_predictions).to_csv(OUTPUTS / "q2_predictions_per_country.csv", index=False)

    return {"n_rows": len(rows)}
```

### 5.10 `src/run_all.py`

```python
"""Orquestador Fase 6: q4 → q1 → q3 → q2 → q5 → q6 → manifest."""

from __future__ import annotations
import json
import subprocess
from datetime import datetime, timezone

from ._common_data import F5_F8_MVP, sha256_file, validate_bundle_hash
from .q4_clustering import run_q4
from .q1_investment import run_q1
from .q3_innovation import run_q3
from .q2_adoption import run_q2
from .q5_population_usage import run_q5
from .q6_public_sector import run_q6

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"


def main():
    print("[Fase 6 v0.4] Iniciando build-all")
    print("[T1] Validando bundle Fase 5 v2.0 (12 archivos)...")
    validate_bundle_hash()

    print("[T2] Q4 clustering...")
    q4_result = run_q4(seed=42, k=4)
    print(f"  Q4 silhouettes: {q4_result}")

    print("[T3] Q1 inversión...")
    q1_result = run_q1(seed=42, n_boot=2000)
    print(f"  Q1: {q1_result}")

    print("[T4] Q3 innovación...")
    q3_result = run_q3(seed=42, n_boot=2000)
    print(f"  Q3: {q3_result}")

    print("[T5] Q2 adopción empresarial...")
    q2_result = run_q2(seed=42)
    print(f"  Q2: {q2_result}")

    print("[T6] Q5 uso población (NUEVO v0.2)...")
    q5_result = run_q5(seed=42, n_boot=2000)
    print(f"  Q5: {q5_result}")

    print("[T7] Q6 sector público (NUEVO v0.2)...")
    q6_result = run_q6(seed=42, n_boot=2000)
    print(f"  Q6: {q6_result}")

    print("[T8] Generando manifest...")
    write_manifest()

    print("[Fase 6 v0.4] build-all completado ✓")


def write_manifest():
    try:
        git_sha = subprocess.check_output(
            ["git", "rev-parse", "HEAD"],
            cwd=str(F5_F8_MVP), stderr=subprocess.DEVNULL,
        ).decode().strip()
    except Exception:
        git_sha = "unknown"

    manifest = {
        "phase6_run_id": f"phase6_{git_sha[:8]}_{datetime.now(timezone.utc).strftime('%Y%m%dT%H%M%S')}",
        "version": "0.1",
        "created_at": datetime.now(timezone.utc).isoformat(),
        "git_sha": git_sha,
        "seed": 42,
        "n_bootstrap": 2000,
        "fdr_method": "benjamini_hochberg",
        "fdr_alpha": 0.05,
        "outputs": {},
        "decisions_log": [
            "F6-A: Q4 → Q1 → Q3 → Q2 → PSM",
            "F6-B: bootstrap-OLS principal, PSM exploratorio caliper 0.20",
            "F6-C: Q3 dual primary + auxiliary Stanford",
            "F6-D: 5×10 fold + bootstrap 2000 + LOOCV",
            "F6-E: separate Y models + consistency table + FDR",
            "F6-F: hyperparams fixed, no GridSearch",
            "F6-G: Q4 N=43 oficial Gower + N=18 complementario Jaccard",
            "F6-I: lenguaje 'asociación' por defecto",
        ],
    }
    for p in sorted(OUTPUTS.glob("*")):
        if p.is_file() and p.name != "fase6_manifest.json":
            manifest["outputs"][p.name] = {
                "sha256": sha256_file(p), "bytes": p.stat().st_size,
            }
    with open(OUTPUTS / "fase6_manifest.json", "w") as f:
        json.dump(manifest, f, indent=2, ensure_ascii=False)


if __name__ == "__main__":
    main()
```

### 5.11 `src/api.py`

```python
"""API estable de Fase 6 para consumo en Fase 7-8."""

from __future__ import annotations
import json
from pathlib import Path

import pandas as pd

from ._common_data import F5_F8_MVP

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"


def load_q1_results() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q1_results.csv")


def load_q1_consistency() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q1_consistency.csv")


def load_q1_psm_pairs() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q1_psm_matched_pairs.csv")


def load_q2_results() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q2_results.csv")


def load_q2_predictions() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q2_predictions_per_country.csv")


def load_q3_results() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q3_results.csv")


def load_q3_consistency() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q3_consistency.csv")


def load_q4_clusters() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q4_clusters.csv")


def load_q4_silhouette() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q4_silhouette_scores.csv")


def load_fase6_manifest() -> dict:
    with open(OUTPUTS / "fase6_manifest.json") as f:
        return json.load(f)


# ===== NUEVOS v0.2 =====

def load_q5_results() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q5_results.csv")


def load_q5_consistency() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q5_consistency.csv")


def load_q5_predictions() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q5_predictions_per_country.csv")


def load_q6_results() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q6_results.csv")


def load_q6_consistency() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q6_consistency.csv")


def load_q6_predictions() -> pd.DataFrame:
    return pd.read_csv(OUTPUTS / "q6_predictions_per_country.csv")
```

### 5.13 `src/q5_population_usage.py` (NUEVO v0.2)

```python
"""Q5 Uso de IA por la población:
- Y candidatas: anthropic_usage_pct, anthropic_collaboration_pct, oxford_ind_adoption_emerging_tech
- Modelos: OLS con bootstrap (regresión continua) + Logistic (clasificación binarizada)
- Reusa Y de Q2_adoption (decisión M)."""

from __future__ import annotations
import numpy as np
import pandas as pd
from sklearn.linear_model import LogisticRegression

from ._common_data import (
    load_bundle, get_x2_controls, get_x1_aggregated, F5_F8_MVP,
)
from ._common_regression import (
    fit_ols_with_bootstrap, fit_ridge_lasso_cv, fdr_benjamini_hochberg,
)
from ._common_classification import (
    binarize_by_median, fit_logistic_cv,
)

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"

Y_VARS_Q5 = [
    "anthropic_usage_pct",
    "anthropic_collaboration_pct",
    "oxford_ind_adoption_emerging_tech",
]


def run_q5(seed: int = 42, n_boot: int = 2000) -> dict:
    bundle = load_bundle()
    fm = bundle["feature_matrix"]
    x1_agg = get_x1_aggregated()
    x2 = get_x2_controls()

    def col_z(c):
        return f"{c}_z" if f"{c}_z" in fm.columns else c
    x1_z = [col_z(c) for c in x1_agg]
    x2_z = [col_z(c) for c in x2]

    rows = []
    rows_consistency = []
    rows_predictions = []

    for y in Y_VARS_Q5:
        if y not in fm.columns:
            continue

        # === OLS regresión continua ===
        ols = fit_ols_with_bootstrap(fm, y, x1_z + x2_z, n_boot=n_boot, seed=seed)
        if ols.get("status") == "ok":
            for x in x1_z + x2_z + ["const"]:
                rows.append({
                    "question": "Q5", "y_var": y, "model": "OLS_full", "x_var": x,
                    "coefficient": ols["coefficients"].get(x),
                    "std_error": ols["std_errors_asymptotic"].get(x),
                    "p_value": ols["pvalues_asymptotic"].get(x),
                    "ci95_lower": ols["bootstrap"].get(x, {}).get("ci95_lower"),
                    "ci95_upper": ols["bootstrap"].get(x, {}).get("ci95_upper"),
                    "p_value_bootstrap": ols["bootstrap"].get(x, {}).get("p_two_sided_bootstrap"),
                    "n_effective": ols["n"], "r2": ols["r2"], "ci_method": "bootstrap_2000",
                    "n_boot": n_boot, "seed": seed,
                })

        # === Logistic clasificación (Y binarizada) ===
        sub = fm[[y, "iso3"] + x1_z + x2_z].dropna()
        if len(sub) >= 20:
            y_bin, threshold = binarize_by_median(sub[y])
            sub = sub.assign(y_binary=y_bin.values)
            log = fit_logistic_cv(sub, "y_binary", x1_z + x2_z, seed=seed)
            if log.get("status") == "ok":
                rows.append({
                    "question": "Q5", "y_var": y,
                    "binarization_threshold_value": float(threshold),
                    "binarization_method": "median",
                    "model": "Logistic", "x_var": "__model_metadata__",
                    "n_effective": log["n"],
                    "n_class_0": log["n_class_0"], "n_class_1": log["n_class_1"],
                    "auc_cv_5fold_mean": log["auc_cv_5fold_mean"],
                    "auc_cv_5fold_std": log["auc_cv_5fold_std"],
                    "auc_loocv": log["auc_loocv"],
                    "model_hyperparams_json": '{"penalty":"l2","C":1.0}', "seed": seed,
                })
                for x, coef in log["logistic_coef"].items():
                    rows.append({
                        "question": "Q5", "y_var": y, "model": "Logistic", "x_var": x,
                        "coefficient": coef, "n_effective": log["n"], "seed": seed,
                    })
                # predictions per country
                X_full = sub[x1_z + x2_z].values
                full_log = LogisticRegression(
                    penalty="l2", C=1.0, max_iter=1000,
                    class_weight="balanced", random_state=seed,
                ).fit(X_full, sub["y_binary"].values)
                probs = full_log.predict_proba(X_full)[:, 1]
                for iso3, prob in zip(sub["iso3"].values, probs):
                    rows_predictions.append({
                        "y_var": y, "iso3": iso3,
                        "p_high_population_usage": float(prob),
                        "model": "Logistic_in_sample", "seed": seed,
                    })

    df = pd.DataFrame(rows)
    OUTPUTS.mkdir(parents=True, exist_ok=True)

    # FDR sobre n_binding cruzando los 3 Y
    focal = "n_binding_z" if "n_binding_z" in fm.columns else "n_binding"
    focal_rows = df[(df["x_var"] == focal) & (df["model"] == "OLS_full") & df["p_value"].notna()]
    if len(focal_rows) > 0:
        pvals = focal_rows["p_value"].values
        fdr_q = fdr_benjamini_hochberg(pvals)
        df.loc[focal_rows.index, "fdr_q_value"] = fdr_q
        df.loc[focal_rows.index, "fdr_significant_05"] = (fdr_q < 0.05)

    df.to_csv(OUTPUTS / "q5_results.csv", index=False)
    pd.DataFrame(rows_predictions).to_csv(
        OUTPUTS / "q5_predictions_per_country.csv", index=False)

    # consistency table
    for x in x1_z:
        x_rows = df[(df["x_var"] == x) & (df["model"] == "OLS_full")]
        n_total = len(x_rows)
        if n_total == 0:
            continue
        n_neg = (x_rows["coefficient"] < 0).sum()
        n_pos = (x_rows["coefficient"] > 0).sum()
        n_sig_05 = (x_rows["p_value"] < 0.05).sum()
        n_sig_fdr = (x_rows.get("fdr_significant_05", pd.Series(False)) == True).sum()
        if n_neg >= max(2, 0.66 * n_total) and n_sig_05 >= 1:
            direction = "robust_negative"
        elif n_pos >= max(2, 0.66 * n_total) and n_sig_05 >= 1:
            direction = "robust_positive"
        elif n_sig_05 == 0:
            direction = "null"
        else:
            direction = "mixed"
        rows_consistency.append({
            "question": "Q5", "x_var": x,
            "n_y_total": n_total, "n_y_negative": int(n_neg), "n_y_positive": int(n_pos),
            "n_y_significant_05": int(n_sig_05),
            "n_y_significant_05_after_fdr": int(n_sig_fdr),
            "direction_summary": direction,
        })
    pd.DataFrame(rows_consistency).to_csv(OUTPUTS / "q5_consistency.csv", index=False)

    return {"n_rows_results": len(df), "n_rows_consistency": len(rows_consistency)}
```

### 5.14 `src/q6_public_sector.py` (NUEVO v0.2)

```python
"""Q6 Uso de IA en sector público:
- Y primarias (5, 100% cobertura): oxford_public_sector_adoption,
  oxford_e_government_delivery, oxford_government_digital_policy,
  oxford_ind_data_governance, oecd_2_indigo_oecd_indigo_score
- Y auxiliar (1, 31/43): oecd_4_digital_gov_oecd_digital_gov_overall (tier_low_n)
- Modelos: OLS con bootstrap + Logistic con clasificación binarizada."""

from __future__ import annotations
import pandas as pd
from sklearn.linear_model import LogisticRegression

from ._common_data import (
    load_bundle, get_x2_controls, get_x1_aggregated, F5_F8_MVP,
)
from ._common_regression import (
    fit_ols_with_bootstrap, fit_ridge_lasso_cv, fdr_benjamini_hochberg,
)
from ._common_classification import (
    binarize_by_median, fit_logistic_cv,
)

OUTPUTS = F5_F8_MVP / "FASE6" / "outputs"

Y_VARS_Q6_PRIMARY = [
    "oxford_public_sector_adoption",
    "oxford_e_government_delivery",
    "oxford_government_digital_policy",
    "oxford_ind_data_governance",
    "oecd_2_indigo_oecd_indigo_score",
]
Y_VARS_Q6_AUXILIARY = [
    "oecd_4_digital_gov_oecd_digital_gov_overall",
]


def run_q6(seed: int = 42, n_boot: int = 2000) -> dict:
    bundle = load_bundle()
    fm = bundle["feature_matrix"]
    x1_agg = get_x1_aggregated()
    x2 = get_x2_controls()

    def col_z(c):
        return f"{c}_z" if f"{c}_z" in fm.columns else c
    x1_z = [col_z(c) for c in x1_agg]
    x2_z = [col_z(c) for c in x2]

    rows = []
    rows_predictions = []

    def fit_and_record(y, tier):
        # OLS bootstrap
        ols = fit_ols_with_bootstrap(fm, y, x1_z + x2_z, n_boot=n_boot, seed=seed)
        if ols.get("status") == "ok":
            for x in x1_z + x2_z + ["const"]:
                rows.append({
                    "question": "Q6", "tier": tier,
                    "y_var": y, "model": "OLS_full", "x_var": x,
                    "coefficient": ols["coefficients"].get(x),
                    "std_error": ols["std_errors_asymptotic"].get(x),
                    "p_value": ols["pvalues_asymptotic"].get(x),
                    "ci95_lower": ols["bootstrap"].get(x, {}).get("ci95_lower"),
                    "ci95_upper": ols["bootstrap"].get(x, {}).get("ci95_upper"),
                    "p_value_bootstrap": ols["bootstrap"].get(x, {}).get("p_two_sided_bootstrap"),
                    "n_effective": ols["n"], "r2": ols["r2"], "ci_method": "bootstrap_2000",
                    "n_boot": n_boot, "seed": seed,
                })

        # Logistic clasificación
        sub = fm[[y, "iso3"] + x1_z + x2_z].dropna()
        if len(sub) >= 20:
            y_bin, threshold = binarize_by_median(sub[y])
            sub = sub.assign(y_binary=y_bin.values)
            log = fit_logistic_cv(sub, "y_binary", x1_z + x2_z, seed=seed)
            if log.get("status") == "ok":
                rows.append({
                    "question": "Q6", "tier": tier, "y_var": y,
                    "binarization_threshold_value": float(threshold),
                    "binarization_method": "median",
                    "model": "Logistic", "x_var": "__model_metadata__",
                    "n_effective": log["n"],
                    "n_class_0": log["n_class_0"], "n_class_1": log["n_class_1"],
                    "auc_cv_5fold_mean": log["auc_cv_5fold_mean"],
                    "auc_cv_5fold_std": log["auc_cv_5fold_std"],
                    "auc_loocv": log["auc_loocv"],
                    "model_hyperparams_json": '{"penalty":"l2","C":1.0}', "seed": seed,
                })
                for x, coef in log["logistic_coef"].items():
                    rows.append({
                        "question": "Q6", "tier": tier, "y_var": y,
                        "model": "Logistic", "x_var": x,
                        "coefficient": coef, "n_effective": log["n"], "seed": seed,
                    })
                X_full = sub[x1_z + x2_z].values
                full_log = LogisticRegression(
                    penalty="l2", C=1.0, max_iter=1000,
                    class_weight="balanced", random_state=seed,
                ).fit(X_full, sub["y_binary"].values)
                probs = full_log.predict_proba(X_full)[:, 1]
                for iso3, prob in zip(sub["iso3"].values, probs):
                    rows_predictions.append({
                        "y_var": y, "tier": tier, "iso3": iso3,
                        "p_high_public_sector_use": float(prob),
                        "model": "Logistic_in_sample", "seed": seed,
                    })

    for y in Y_VARS_Q6_PRIMARY:
        if y in fm.columns:
            fit_and_record(y, tier="primary")
    for y in Y_VARS_Q6_AUXILIARY:
        if y in fm.columns:
            fit_and_record(y, tier="auxiliary_low_n")

    df = pd.DataFrame(rows)

    # FDR solo sobre tier=primary
    focal = "n_binding_z" if "n_binding_z" in fm.columns else "n_binding"
    primary_focal = df[
        (df["x_var"] == focal) & (df["model"] == "OLS_full") &
        (df["tier"] == "primary") & df["p_value"].notna()
    ]
    if len(primary_focal) > 0:
        pvals = primary_focal["p_value"].values
        fdr_q = fdr_benjamini_hochberg(pvals)
        df.loc[primary_focal.index, "fdr_q_value"] = fdr_q
        df.loc[primary_focal.index, "fdr_significant_05"] = (fdr_q < 0.05)

    df.to_csv(OUTPUTS / "q6_results.csv", index=False)
    pd.DataFrame(rows_predictions).to_csv(
        OUTPUTS / "q6_predictions_per_country.csv", index=False)

    # consistency tier=primary
    cons = []
    for x in x1_z:
        sub = df[(df["x_var"] == x) & (df["model"] == "OLS_full") & (df["tier"] == "primary")]
        if len(sub) == 0:
            continue
        n_neg = (sub["coefficient"] < 0).sum()
        n_pos = (sub["coefficient"] > 0).sum()
        n_sig = (sub["p_value"] < 0.05).sum()
        cons.append({
            "question": "Q6", "tier": "primary", "x_var": x,
            "n_y_total": len(sub),
            "n_y_negative": int(n_neg),
            "n_y_positive": int(n_pos),
            "n_y_significant_05": int(n_sig),
        })
    pd.DataFrame(cons).to_csv(OUTPUTS / "q6_consistency.csv", index=False)

    return {"n_rows": len(df)}
```

---

## 6. Output schemas (long-format auditables)

### 6.1 `q1_results.csv` y `q3_results.csv`

```
question, y_var, model, x_var, coefficient, std_error, p_value, p_value_bootstrap,
ci95_lower, ci95_upper, n_effective, r2, r2_adj,
cv_r2_mean_5fold, cv_r2_std_5fold, loocv_r2,
ci_method, n_boot, fdr_q_value, fdr_significant_05,
seed, model_hyperparams_json, tier (solo Q3)
```

`x_var = "__model_metadata__"` para filas que no tienen un X específico (ej. fila resumen del modelo solo-controles).

### 6.2 `q1_consistency.csv` / `q3_consistency.csv`

```
question, x_var, n_y_total, n_y_negative, n_y_positive,
n_y_significant_05, n_y_significant_05_after_fdr,
direction_summary, tier (solo Q3)
```

`direction_summary ∈ {robust_negative, robust_positive, mixed, null}` por reglas:
- `robust_*`: ≥66% de Y mismo signo Y ≥1 significativo a 0.05
- `null`: 0 significativos
- `mixed`: cualquier otro caso

### 6.3 `q1_psm_matched_pairs.csv`

```
treated_iso3, control_iso3, propensity_treated, propensity_control,
ps_diff, caliper, treatment_col, status
```

`status ∈ {ok, exploratory_low_n, insufficient_n}`. Si `insufficient_n`, archivo tiene una fila marker.

### 6.4 `q2_results.csv`

```
question, y_var, binarization_threshold_value, binarization_method,
model, x_var, coefficient_or_importance,
n_effective, n_class_0, n_class_1,
auc_cv_5fold_mean, auc_cv_5fold_std, auc_loocv,
confusion_tp, confusion_tn, confusion_fp, confusion_fn,
model_hyperparams_json, seed
```

### 6.5 `q2_predictions_per_country.csv`

```
y_var, iso3, p_high_adoption, model, seed
```

Una fila por (Y, país). 7 Y × 43 países máx.

### 6.6 `q4_clusters.csv`

```
iso3, country_name_canonical, region,
cluster_kmeans_n43, cluster_hca_n43,
cluster_kmeans_n18, cluster_hca_n18,
in_iapp_subset
```

`cluster_*_n18` es NaN para 25 países sin IAPP — eso es información, no missingness.

### 6.7 `q4_distance_matrix_n43.csv` y `q4_distance_matrix_n18.csv`

```
iso3_a, iso3_b, distance, distance_metric, scope
```

Long-format triplete (i<j) — 903 filas para n=43, 153 para n=18.

### 6.8 `q4_silhouette_scores.csv`

```
method, scope, k, silhouette_score, n_observations
```

4 filas mínimo: kmeans/hca × n43_mixed/n18_binary.

### 6.9 `q5_results.csv` (NUEVO v0.2)

Mismo schema que `q1_results.csv` con `question="Q5"`. 3 Y candidatas. Modelos: `OLS_full`, `Logistic`. Filtros sub-pregunta="Q5" en consumo Fase 8.

### 6.10 `q5_consistency.csv` (NUEVO v0.2)

Mismo schema que `q1_consistency.csv` con `question="Q5"`. Una fila por X1 agregada con `direction_summary`.

### 6.11 `q5_predictions_per_country.csv` (NUEVO v0.2)

```
y_var, iso3, p_high_population_usage, model, seed
```
3 Y × 43 países máx (≈129 filas).

### 6.12 `q6_results.csv` (NUEVO v0.2)

Mismo schema que `q3_results.csv` (incluye columna `tier ∈ {primary, auxiliary_low_n}`) con `question="Q6"`. 5 Y primarias + 1 auxiliar. Modelos: `OLS_full`, `Logistic`.

### 6.13 `q6_consistency.csv` (NUEVO v0.2)

Mismo schema que `q3_consistency.csv` con `question="Q6"` y filas solo para `tier="primary"`.

### 6.14 `q6_predictions_per_country.csv` (NUEVO v0.2)

```
y_var, tier, iso3, p_high_public_sector_use, model, seed
```
5 Y_primary × 43 + 1 Y_aux × 31 = ≈246 filas.

### 6.15 Reducción dimensional — fuera de scope (v0.4)

Fase 6 NO produce ningún CSV con prefijo `pca_*`. Si aparece tal archivo en `FASE6/outputs/`, hay un bug. Análisis exploratorio de varianza/redundancia ya está en `FASE4/outputs/eda_principal/` (`eda_redundancy_report.csv`, `eda_inter_block_correlations.csv`, `eda_candidates_for_feature_engineering.csv`); el notebook 06 puede mostrar esos archivos como contexto narrativo de Q4, sin recomputar nada.

### 6.19 `fase6_manifest.json`

```json
{
  "phase6_run_id": "phase6_<git_sha8>_<UTC_timestamp>",
  "version": "0.4",
  "git_sha": "...",
  "seed": 42,
  "n_bootstrap": 2000,
  "fdr_method": "benjamini_hochberg",
  "fdr_alpha": 0.05,
  "n_subpreguntas": 6,
  "outputs": {
    "q1_results.csv": {"sha256": "...", "bytes": 12345},
    "q5_results.csv": {"sha256": "...", "bytes": "..."},
    "q6_results.csv": {"sha256": "...", "bytes": "..."},
    "...": {"...": "..."}
  },
  "decisions_log": ["F6-A: ...", "F6-K: Q6 expandida con 6 vars Fase 5",
                    "F6-L: PCA fuera de scope (Fase 4 cubre análisis exploratorio)",
                    "F6-M: Q2/Q5/Q6 reuso Y"]
}
```

---

## 7. Notebook `06_modeling.ipynb`

**Regla:** el notebook NO recomputa modelos. Solo lee CSVs de `outputs/` y exhibe.

```text
SECCIÓN 1 — Validación de integridad (10 min reading)
  • Cell: imports + load_bundle() + validate_bundle_hash()
  • Cell: tabla de hashes esperados vs actuales (incluye expansión v0.2)

SECCIÓN 2 — Contexto descriptivo desde Fase 4 (lectura, no recomputo)
  • Markdown: nota — "Análisis exploratorio de varianza/redundancia ya cubierto en
    Fase 4 (eda_redundancy_report.csv, eda_inter_block_correlations.csv).
    Esta sección lo cita; no se ejecuta PCA en Fase 6 (Decisión L)."
  • Cell: render de tabla resumen redundancy_report (top 20 pares con |ρ|≥0.85)
  • Cell: render de heatmap inter-block correlations
  • Cell: lista de variables marcadas como collapse_redundant / exclude_low_coverage en Fase 4

SECCIÓN 3 — Q4 Tipos regulatorios (clustering)
  • Cell: load_q4_clusters() + load_q4_silhouette()
  • Cell: dendrograma HCA n43
  • Cell: tabla "Países por cluster" con interpretación
  • Cell: posición de Chile en cluster
  • Cell: comparación cluster_n43 vs cluster_n18 (qué cluster cae el subset IAPP)

SECCIÓN 4 — Q1 Inversión
  • Cell: load_q1_results() + load_q1_consistency()
  • Cell: forest plot de coeficientes (n_binding sobre 5-6 Y) con IC95 bootstrap
  • Cell: tabla "OLS solo controles" vs "OLS + X1" (mejora R²)
  • Cell: load_q1_psm_pairs() — tabla pares con status
  • Cell: interpretación de q1_consistency.csv (direction_summary por X1)

SECCIÓN 5 — Q3 Innovación
  • Cell: load_q3_results() + load_q3_consistency()
  • Cell: forest plot Q3 primary
  • Cell: comparación Q3 primary vs auxiliary (¿cambia el signo con Stanford?)
  • Cell: scatter Y_predicted vs Y_real (Ridge model)

SECCIÓN 6 — Q2 Adopción empresarial
  • Cell: load_q2_results() + load_q2_predictions()
  • Cell: ROC curves Logistic vs RF (por Y)
  • Cell: Confusion matrices
  • Cell: feature_importance bar plot del RF
  • Cell: tabla "Probabilidad de alta adopción" para Chile y peers

SECCIÓN 7 — Q5 Uso IA población (NUEVO v0.2)
  • Cell: load_q5_results() + load_q5_consistency() + load_q5_predictions()
  • Cell: forest plot Q5 sobre 3 Y (anthropic_usage, anthropic_collab, oxford_emerging_tech)
  • Cell: tabla direction_summary por X1
  • Cell: ROC curves Logistic Q5
  • Cell: tabla P(alto_uso_población) por país: Chile + AI leaders + LATAM peers

SECCIÓN 8 — Q6 Uso IA sector público (NUEVO v0.2)
  • Cell: load_q6_results() + load_q6_consistency() + load_q6_predictions()
  • Cell: forest plot Q6 primary (5 Y) — markdown: "Y exclusivo sector público"
  • Cell: forest plot Q6 auxiliary (Stanford INDIGO con N=31)
  • Cell: comparación primary vs auxiliary
  • Cell: tabla P(alto_uso_sector_público) por país
  • Cell: caso Chile específico — qué tan IA-público es Chile vs SGP/EST/IRL

SECCIÓN 9 — Síntesis preliminar (no es Fase 8)
  • Cell: tabla resumen Q1-Q6 (1 sentencia por Q en lenguaje "asociación")
  • Cell: tabla "consistencia cross-Q" — ¿n_binding tiene mismo signo en Q1, Q3, Q5, Q6?
  • Cell: load_fase6_manifest() — verificación final de outputs

Cada sección comienza con markdown explicando QUÉ se ve, SIN interpretación causal.
```

---

## 8. Tests obligatorios

```text
F5_F8_MVP/FASE6/tests/
├── conftest.py                         # fixtures que cargan bundle
├── test_bundle_integrity.py            # SHA-256 del bundle phase6_ready
├── test_split_integrity.py             # 34/9 disjoint, CHL en train (Decisión H)
├── test_q1_investment.py               # schema, n_effective, bootstrap, PSM status
├── test_q2_adoption.py                 # schema, AUC en rango, balance binarización
├── test_q3_innovation.py               # schema, tier separation, primary N≥30
├── test_q4_clustering.py               # 43 países en n43, 18 en n18, silhouette en rango
└── test_outputs_no_causal_language.py  # lint outputs no contienen "causa", "produces"
```

### 8.1 Tests críticos por sub-pregunta

```python
# test_q1_investment.py
def test_q1_results_schema(q1_results):
    expected_cols = {
        "question", "y_var", "model", "x_var",
        "coefficient", "n_effective", "seed",
    }
    assert expected_cols.issubset(q1_results.columns)

def test_q1_n_effective_per_y(q1_results):
    for y in q1_results["y_var"].unique():
        sub = q1_results[(q1_results["y_var"] == y) & (q1_results["model"] == "OLS_full")]
        if len(sub) > 0:
            n = sub["n_effective"].iloc[0]
            assert n >= 15 or sub["model"].iloc[0] == "insufficient_n"

def test_q1_bootstrap_ci_finite(q1_results):
    sub = q1_results[(q1_results["model"] == "OLS_full") & q1_results["ci95_lower"].notna()]
    assert sub["ci95_lower"].apply(np.isfinite).all()
    assert sub["ci95_upper"].apply(np.isfinite).all()

def test_q1_psm_status_valid(q1_psm_pairs):
    valid = {"ok", "exploratory_low_n", "insufficient_n"}
    assert q1_psm_pairs["status"].isin(valid).all()

def test_q1_consistency_direction_valid(q1_consistency):
    valid = {"robust_negative", "robust_positive", "mixed", "null"}
    assert q1_consistency["direction_summary"].isin(valid).all()


# test_q4_clustering.py
def test_q4_clusters_n43_complete(q4_clusters):
    assert len(q4_clusters) == 43
    assert q4_clusters["cluster_kmeans_n43"].notna().all()
    assert q4_clusters["cluster_hca_n43"].notna().all()

def test_q4_clusters_n18_subset(q4_clusters):
    n18 = q4_clusters["cluster_kmeans_n18"].notna().sum()
    assert n18 == q4_clusters["in_iapp_subset"].sum()

def test_q4_chile_in_clusters(q4_clusters):
    chile = q4_clusters[q4_clusters["iso3"] == "CHL"]
    assert len(chile) == 1
    assert pd.notna(chile["cluster_kmeans_n43"].iloc[0])

def test_q4_silhouette_in_range(q4_silhouette):
    assert q4_silhouette["silhouette_score"].between(-1, 1).all()


# test_split_integrity.py (Decisión H)
def test_split_disjoint_and_complete():
    s = pd.read_csv(FASE5_BUNDLE / "phase6_train_test_split.csv")
    assert len(s) == 43
    assert s["iso3"].nunique() == 43
    assert set(s["split"].unique()) == {"train", "test"}
    counts = s["split"].value_counts().to_dict()
    assert counts["train"] == 34 and counts["test"] == 9
    assert "CHL" in s["iso3"].values
    chile_split = s[s["iso3"] == "CHL"]["split"].iloc[0]
    assert chile_split == "train"


# test_outputs_no_causal_language.py (Decisión I)
def test_no_causal_words_in_csv_columns():
    forbidden = ["causes", "produces", "impact", "causa", "produce"]
    for csv_path in OUTPUTS.glob("*.csv"):
        df = pd.read_csv(csv_path, nrows=5)
        for col in df.columns:
            for word in forbidden:
                assert word.lower() not in col.lower(), \
                    f"{csv_path.name}.{col} contiene palabra prohibida '{word}'"
```

---

## 9. Order de implementación detallado (v0.2)

```text
Paso 0 — Expansión Fase 5 (2 horas) [PRECONDICIÓN PARA Q5/Q6]
  ✓ Actualizar FASE5/config/mvp_variables.yaml: 40 → 46 variables
    Agregar: oxford_e_government_delivery, oxford_government_digital_policy,
             oxford_ind_data_governance, oxford_governance_ethics,
             oecd_2_indigo_oecd_indigo_score, oecd_4_digital_gov_oecd_digital_gov_overall
  ✓ Re-ejecutar FASE5/src/build.py para regenerar feature_matrix_mvp.csv (43 × ~138)
  ✓ Re-generar bundle phase6_ready/ (12 archivos con nuevos hashes)
  ✓ Actualizar phase6_modeling_contract.yaml con Y_Q5 y Y_Q6
  ✓ Bumpear versión Fase 5: 1.0 → 2.0
  ✓ pytest FASE5/tests/ PASA (tests adaptados a 46 vars)
  ✓ Validar: las 46 vars preservadas byte-a-byte

Paso 1 — Setup FASE6 (30 min)
  ✓ Crear F5_F8_MVP/FASE6/ esqueleto + pyproject.toml + Makefile
  ✓ Llenar config/fase6_decisions.yaml con A-M firmadas (incluye K, L, M v0.2)
  ✓ Llenar config/fase6_models.yaml con hyperparams fijos
  ✓ Crear tests/conftest.py + test_bundle_integrity.py + test_split_integrity.py
  ✓ Verificar: pytest test_bundle_integrity.py + test_split_integrity.py PASA

Paso 2 — Utilidades comunes (45 min)
  ✓ Implementar src/_common_data.py
  ✓ Implementar src/_common_regression.py
  ✓ Implementar src/_common_classification.py
  ✓ Smoke test sobre cada utilidad

Paso 3 — Q4 Clustering (1 hora)
  ✓ Implementar src/q4_clustering.py
  ✓ Generar q4_clusters.csv, q4_distance_matrix_*.csv, q4_silhouette_scores.csv, q4_centroids.csv
  ✓ Test test_q4_clustering.py PASA

Paso 4 — Q1 Investment (2 horas)
  ✓ Implementar src/q1_investment.py incluyendo PSM exploratorio
  ✓ Generar q1_results.csv, q1_consistency.csv, q1_psm_matched_pairs.csv
  ✓ Test test_q1_investment.py PASA

Paso 5 — Q3 Innovation (1 hora)
  ✓ Implementar src/q3_innovation.py
  ✓ Generar q3_results.csv, q3_consistency.csv
  ✓ Test test_q3_innovation.py PASA

Paso 6 — Q2 Adoption empresarial (1 hora)
  ✓ Implementar src/q2_adoption.py
  ✓ Generar q2_results.csv, q2_predictions_per_country.csv
  ✓ Test test_q2_adoption.py PASA

Paso 7 — Q5 Uso población (NUEVO v0.2, 1 hora)
  ✓ Implementar src/q5_population_usage.py (reusa _common_regression + _common_classification)
  ✓ Generar q5_results.csv, q5_consistency.csv, q5_predictions_per_country.csv
  ✓ Test test_q5_population_usage.py PASA

Paso 8 — Q6 Sector público (NUEVO v0.2, 45 min)
  ✓ Implementar src/q6_public_sector.py (con tier primary/auxiliary)
  ✓ Generar q6_results.csv, q6_consistency.csv, q6_predictions_per_country.csv
  ✓ Test test_q6_public_sector.py PASA

Paso 9 — Orquestador y manifest (30 min)
  ✓ Implementar src/run_all.py (q4 → q1 → q3 → q2 → q5 → q6 → manifest)
  ✓ Implementar src/api.py (load_q*_*)
  ✓ Generar fase6_manifest.json
  ✓ Test test_outputs_no_causal_language.py PASA

Paso 10 — Notebook (1.5 horas)
  ✓ Crear notebooks/06_modeling.ipynb con 9 secciones (incluye Q5, Q6, contexto Fase 4)
  ✓ Solo lee de outputs/, no recomputa
  ✓ Ejecuta cell-by-cell sin errores

Paso 11 — README + cierre (30 min)
  ✓ README.md con inputs/outputs/decisiones A-M/cómo correr
  ✓ Makefile: make fase6-all → make fase6-test → make fase6-notebook
  ✓ Verificar reproducibilidad byte-a-byte

TOTAL: ≈9 horas (8h v0.1 + 1.75h Q5/Q6 implementación − 0.75h por eliminación PCA)

NOTA v0.4: El "Paso 0 Expansión Fase 5" (≈1.5h) ya NO es parte del timeline de Fase 6.
Esa expansión vive en CONTEXTOS/5.0.PLAN_FASE5_V2_UPDATE.md y debe estar 100%
completada antes de iniciar este plan. Asume que phase6_ready/ tiene
12 archivos (los originales actualizados con 46 vars y contrato Q5/Q6) hashes verificados.
NO existen archivos pca_* en el bundle (Decisión L; análisis exploratorio cubierto por Fase 4).
```

---

## 10. Criterios de aceptación (auditable)

```
FASE 6 — ACEPTACIÓN MVP

Bundle de inputs
[ ] phase6_ready_manifest hashes coinciden con archivos físicos (test_bundle_integrity)
[ ] feature_matrix shape == (43, 138) confirmado en test
[ ] split: 34 train + 9 test, disjoint, CHL en train (test_split_integrity)
[ ] Cero recompute de Fase 5: hashes Fase 5 inalterados post-Fase 6

Implementación
[ ] config/fase6_decisions.yaml firmado por humano con A-J
[ ] config/fase6_models.yaml con hyperparams fijos (no tuneados)
[ ] src/run_all.py ejecuta end-to-end sin error
[ ] make fase6-all reproducible byte-a-byte con misma seed

Outputs Q1 (Inversión)
[ ] q1_results.csv: 5-6 Y × {OLS, Ridge, Lasso} pobladas
[ ] Cada coeficiente en OLS_full tiene IC95 bootstrap finito
[ ] FDR Benjamini-Hochberg aplicado a p-values del X1 focal cruzando los 5-6 Y
[ ] q1_consistency.csv: ≥1 fila por X1 agregada con direction_summary válido
[ ] q1_psm_matched_pairs.csv: status ∈ {ok, exploratory_low_n, insufficient_n}

Outputs Q2 (Adopción)
[ ] q2_results.csv: ≥3 Y × {Logistic, RF} pobladas
[ ] AUC cv 5-fold + LOOCV reportados
[ ] Confusion matrix por modelo+Y reportado
[ ] q2_predictions_per_country.csv: 1 fila por (Y, país) para Y exitosos

Outputs Q3 (Innovación)
[ ] q3_results.csv con tier ∈ {primary, auxiliary_low_n}
[ ] Ridge + GBR para cada Y primary
[ ] q3_consistency.csv análogo a q1
[ ] FDR aplicado solo en tier=primary

Outputs Q4 (Clustering)
[ ] q4_clusters.csv: cluster_*_n43 completo para 43 países
[ ] cluster_*_n18 NaN para los 25 sin IAPP, valor para los 18 con IAPP
[ ] q4_silhouette_scores.csv: 4 filas mínimo
[ ] CHL aparece en cluster_n43 (oficial)

Outputs Q5 (Uso población — NUEVO v0.2)
[ ] q5_results.csv: 3 Y × {OLS_full, Logistic} pobladas
[ ] Cada coeficiente OLS tiene IC95 bootstrap finito
[ ] FDR aplicado al X1 focal cruzando los 3 Y
[ ] q5_consistency.csv: ≥1 fila por X1 con direction_summary válido
[ ] q5_predictions_per_country.csv: P(alto_uso_población) por (Y, país)

Outputs Q6 (Sector público — NUEVO v0.2)
[ ] q6_results.csv con tier ∈ {primary, auxiliary_low_n}
[ ] 5 Y primarias × {OLS_full, Logistic} pobladas
[ ] 1 Y auxiliar (oecd_4_digital_gov_overall) con tier="auxiliary_low_n"
[ ] FDR aplicado solo en tier=primary
[ ] q6_consistency.csv: solo filas tier=primary
[ ] q6_predictions_per_country.csv: P(alto_uso_sector_público) por (Y, país)

Reducción dimensional fuera de scope (Decisión L, v0.4)
[ ] FASE6/outputs/ NO contiene archivos pca_*.csv ni similares
[ ] FASE6/src/ NO contiene módulo _common_diagnostics.py
[ ] FASE6/tests/ NO contiene test_pca_diagnostics.py
[ ] Ningún src/*.py importa sklearn.decomposition.PCA
[ ] Notebook 06_modeling.ipynb sección 2 muestra contexto descriptivo de Fase 4
    (eda_redundancy_report, eda_inter_block_correlations) sin recomputar

Expansión Fase 5 (NUEVO v0.2)
[ ] FASE5/config/mvp_variables.yaml actualizado: 46 variables
[ ] feature_matrix_mvp.csv regenerado con 6 vars adicionales
[ ] Bundle phase6_ready/ regenerado con nuevos hashes
[ ] phase6_modeling_contract.yaml actualizado con Y_Q5 y Y_Q6
[ ] Versión Fase 5: 1.0 → 2.0 (minor backwards-compatible)
[ ] Las 40 vars originales preservadas byte-a-byte (smoke test)
[ ] FASE5 tests siguen pasando

Lenguaje (Decisión I)
[ ] test_outputs_no_causal_language.py PASA
[ ] README de FASE6 usa "asociación" no "efecto"

Tests
[ ] pytest FASE6/tests/ -v: 100% PASS
[ ] Cobertura ≥80% sobre src/

Notebook
[ ] 06_modeling.ipynb ejecuta sin error cell-by-cell
[ ] Notebook NO recomputa modelos: solo lee de outputs/

Reproducibilidad
[ ] seed=42 en todo modelo estocástico
[ ] fase6_manifest.json contiene git_sha + sha de cada output
[ ] Re-correr `make fase6-all` reproduce manifest hashes byte-a-byte (golden test)

Trazabilidad
[ ] Cada decisión A-J en config/fase6_decisions.yaml con justificación
[ ] Limitaciones documentadas en README: N=18 IAPP, N=26 Stanford, PSM exploratorio
```

---

## 11. Riesgos y mitigaciones

| Riesgo | Probabilidad | Mitigación |
|---|---|---|
| Bootstrap con N=18 da IC inestables | Alta | Reportar n_boot_valid; si <100, marcar status="insufficient" |
| PSM no encuentra ≥5 pares con caliper 0.20 | Alta | Marcar `status="exploratory_low_n"` o `"insufficient_n"`; reporte ejecutivo no cita PSM |
| FDR sobre 5-6 Y elimina toda significancia | Media | Reportar pre-FDR y post-FDR; consistency table es honest summary |
| Random Forest sobreajusta con N=20-25 | Alta | max_depth=4, min_samples_leaf=3, n_estimators=300; comparar AUC train vs CV |
| LOOCV diverge mucho de 5-fold | Media | Reportar ambos en mismo CSV; documentar inestabilidad como hallazgo |
| Stanford en Q3 cambia signos | Media-alta | Tier separation deja al lector ver el sesgo; FDR solo en tier=primary |
| Multicolinealidad X1 (n_binding ↔ regulatory_intensity) | Alta | Lasso identifica; reportar VIF en Q1 si statsmodels disponible |
| Cluster N=43 con todos-cero genera cluster artificial | Media | Cluster N=18 complementario muestra estructura "real" |
| Notebook se rompe en otro entorno | Baja | Notebook solo lee CSVs; no depende de modelado en vivo |
| Hashes Fase 5 cambian accidentalmente | Baja | test_no_phase5_mutation comparando manifest entrada/salida |

---

## 12. Lo que Fase 6 NO entrega (transparencia)

Documento explícito para el README y para el Executive Summary de Fase 8:

1. **No hay sensitivity excluyendo USA** — eso es Fase 7
2. **No hay comparación con baseline trivial / solo-controles formal** — Fase 7 lo formaliza (en Fase 6 solo se computa el OLS_only_X2 como referencia)
3. **No hay GridSearchCV de hyperparams** — defaults fijos por reproducibilidad; tuning solo en Fase 7 como prueba de estabilidad
4. **No hay modelo causal robusto** — solo PSM exploratorio. DiD/IV/SCM son post-MVP
5. **No hay análisis temporal** — solo snapshot 2025-2026
6. **No hay NLP del corpus legal** — fuera de scope MVP
7. **No hay reporte ejecutivo, ni Excel final, ni recomendaciones para Chile** — eso es Fase 8

---

## 13. Próximos pasos post-Fase 6

1. **Fase 7 Evaluation**: sensitivity + baselines + stability check + hyperparameter robustness
2. **Fase 8 Reporting**: notebook maestro + EXECUTIVE_SUMMARY.md + Excel auditable final
3. **Tag git** `mvp-v0.1` después de Fase 8

---

## 14. Verificación end-to-end

```bash
cd /home/pablo/Research_LeyIA_DataScience/Research_AI_law/F5_F8_MVP/FASE6/

# 1. Pipeline completo
python -m src.run_all
# Esperado: "[Fase 6] build-all completado ✓"

# 2. Tests
pytest tests/ -v
# Esperado: 100% PASS

# 3. Validar reproducibilidad
make fase6-all
sha256sum outputs/q1_results.csv outputs/q4_clusters.csv > /tmp/hashes_run1.txt
rm -rf outputs/*
make fase6-all
sha256sum outputs/q1_results.csv outputs/q4_clusters.csv > /tmp/hashes_run2.txt
diff /tmp/hashes_run1.txt /tmp/hashes_run2.txt
# Esperado: vacío (reproducibilidad byte-a-byte)

# 4. Notebook
jupyter nbconvert --to notebook --execute notebooks/06_modeling.ipynb --output 06_modeling_executed.ipynb
# Esperado: ejecuta sin error

# 5. Verificar Fase 5 intacta
python -c "
from FASE5.src.api import load_phase6_ready_manifest
import hashlib
manifest = load_phase6_ready_manifest()
for fname, meta in manifest['files'].items():
    p = f'F5_F8_MVP/FASE5/outputs/phase6_ready/{fname}'
    h = hashlib.sha256(open(p,'rb').read()).hexdigest()
    assert h == meta['sha256'], f'MUTATED: {fname}'
print('Fase 5 intacta — cero mutación')
"
```

---

## 15. Filosofía final del modelado

> Fase 6 no busca el "modelo perfecto". Busca el modelo más **simple, reproducible y defendible** dado el N y los datos disponibles.

Tres reglas de oro al implementar:

1. **Cuando dudes entre dos modelos, elige el más simple.** OLS antes que GBR si ambos dan signo igual.
2. **Cuando el N sea pequeño, reporta más, no menos.** Bootstrap, LOOCV, sensitivity — todo dato adicional sobre la estabilidad es honesto.
3. **Cuando el lenguaje sea ambiguo, sé conservador.** "Asociación" antes que "efecto". El jefe puede pedir más; nunca podrás recuperar credibilidad si overclaimaste.

Cuando el jefe pregunte: "¿De dónde sale este coeficiente?", la respuesta debe ser:

> `q1_results.csv` fila X (y_var=Y, model=M, x_var=Z). Bootstrap 2000 iter sobre OLS, seed=42. N efectivo=N, IC95 [a, b]. Reproducible vía `make fase6-all`.

Si esa respuesta no se puede dar, el resultado no entra a la fase.

Fin del documento.
